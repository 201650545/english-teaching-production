# M5 语法库验收报告（grammar_bank.json）

> 模块：M5 语法库 · 验收时间：2026-08-02 · 生成器：`build_grammar_bank.py`（v2）
> 数据源：`许颖嘉\内容蓝图\单课\第XX课_内容蓝图.md`「二、语法详案」+ `lesson_map.json` grammar 字段
> 红线合规：未改 `courseware_core.py`、未动旧资产；学生材料未提入学分数；JSON 无注释

## 一、总量与字段

- 考点总数 **54 个**，覆盖 L1–L21 语法期（`lesson_map.json` grammar 字段对齐）
- 填实 **54/54**，未填 **0**；每考点六字段齐全：`构成 / 例句6 / 易错5 / 口诀 / 六色卡(6键) / 中考考法`
- L5 验收三考点（祈使句基础 / What特殊疑问句 / like的用法）六字段齐全 ✓，例句均满 6 条

## 二、v2 质量修复（本轮）

| 问题 | 修复 |
|---|---|
| 例句列含 `---；---`、`**me**；I→me` 等表格抽取脏数据 | 例句抽取仅取表头含「示例/例句」的表；分隔行不打断表格；列索引去首尾管道偏移 |
| 例句混入「速读抓主旨/精读划关键词」等阅读策略行 | 例句过滤：拒绝 `---`、`**`、`占位`、`待补`、阅读策略关键词 |
| 练习结构蓝图等编号行泄漏进语法③例句 | 语法块截断到下一「## 」顶级节 |
| `中考考法/六色卡/易错5` 残留 `**` 加粗标记 | 全字段统一 `clean_md` 清洗 |

修复后全库扫描：**脏例句 0**、**markdown 残留 0**。

## 三、质量统计（诚实口径）

| 指标 | 数量 | 说明 |
|---|---|---|
| 例句满 6 条（真实例句） | **45/54** | 9 个考点 3–5 条（见下），源蓝图例句本身偏少，未编造补齐 |
| 例句不足的考点 | 9 个 | 人称代词主格与宾格(3)、形容词性/名词性物主代词(3)、be动词与人称搭配(3)、指示代词(4)、tidy/messy(5)、一般过去时was/were(5)、一般现在时三单-s(5)、名词不规则复数(5)、主谓一致(5) |
| 易错 ≥4 条 | **32/54** | 22 个考点仅 3 条，源蓝图「易错点」即提供 3 条，属软性缺口 |
| 越级残留（英文 FORBIDDEN 词表） | **0** | 越级清洗 3 个考点后，L5 课件 verify_v2 防越级未命中 ✓ |
| 中文「比较级/最高级」字样 | 4 处 | 全部出现在「防越级约束」说明中（如"仅限原级，严禁比较级"），属正确教学内容，非越级 |

## 四、需教师决策点

1. **G15 like 的用法**：第05课内容蓝图 G15 为 `like + to do`（防越级约束明确"不引入 like doing，留至 L8–L9 对比"），语法库已按蓝图填 `like to do`；但 L5 课件模板（`courseware_engine.py` 一览表/改错专练/综合梳理）多处使用 `like doing`。**以哪个为准需确认**：a) 课件模板改 `like to do`（贴合蓝图螺旋链 L5 to do→L6 want to do→L13 would like to do）；b) 蓝图改 `like doing`。
2. 9 个考点例句不足 6 条、22 个考点易错仅 3 条：若要求补齐，属内容撰写任务（建议后续按课补蓝图），工具层不阻塞。

## 五、验证命令

```powershell
cd D:\英语教学\00_工具
python build_grammar_bank.py          # 填实 54/54，越级清洗 3
python courseware_engine.py           # L5 课件（grammar 内容更新后）
python verify_v2.py test_L5_courseware.html   # 应全 PASS
```
