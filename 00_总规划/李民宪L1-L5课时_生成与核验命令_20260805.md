# 李民宪 L1–L5 课时 · 生成与核验命令（2026-08-05）

**执行方**：可读取 `D:\英语教学` 本地文档的 Agent（原执行者）
**目标**：把李民宪 **L1–L5** 五课补齐并按新规范达标。
**现状盘点（2026-08-05 实测）**：

| 课 | 现状 | 本命令要求 |
|---|---|---|
| L1 | 课件/练习已生成，但未满足新规范（合同标记缺失、阅读词数<120、三篇全单选、选择占比 76%） | **核验 + 修复**（先落《李民宪L1课时包_补充修复命令_20260804.md》4 项，再核验新规范） |
| L2 | 已有 课件成品_网页PPT(_v2) + 配套练习_Word | **核验**；不合格按新规范修 |
| L3 | 已有 课件成品_网页PPT | **核验**；不合格按新规范修 |
| L4 | **完全缺失**（无目录） | **按新规范完整生成**（课件 + 配套练习） |
| L5 | 已有 课件成品_网页PPT + 契约 + 配套练习 | **核验**；不合格按新规范修 |

---

## 〇、输入材料（必读）

- 培优规划：`李民宪\内容蓝图\全局\00_课程规划_25课时.md`（25 课时逐课方案）
- 词汇真源：**750 词矩阵**（每课新词按矩阵，零重复铁律）
- 教材口径：2025 秋新版八上（双轨，教师已拍板）
- 规范：`00_格式规范\04`（§四之二 30词模型 / §四之三 教学容量 / §四之四 题型配比与选择题占比 / 档位B 阅读基线）、`01`（§3.14 / 主题化 / 六色卡）、`07`（29 游戏）、`08`（数据闭环）
- 交互菜单：`00_总规划\_GPT交互菜单包_20260805\06_互动菜单与校验\interaction_menu_v1.json`
- 校验：`00_工具\verify_v2.py`、`verify_interaction_v1.py`（含 VIS-613）
- 补充修复：`00_总规划\李民宪L1课时包_补充修复命令_20260804.md`（L1 专用）

## 一、视觉风格（打破红橙统一）

- **五课各用独立主色系**，互不相同、不用红金默认：L1=紫、L2=青、L3=绿、L4=琥珀/金棕、L5=蓝。
- 封面模板 / 词汇卡变体按课取不同 variant（相邻课不重复）。
- 结构用引擎统一骨架，视觉差异来自 主色/背景纹理/封面/组件变体；不得整课沿用默认红金全套。

## 二、内容（培优新规范硬性）

- **30 词模型**：每课 20 复习词（4 类各 5）+ 10 新词（6-7 核心产出 + 3-4 理解）。
- **新词按 750 词矩阵取**，全课零重复。
- **档位 B 阅读基线**：语篇 **120–160 词**、生词率 **≤12%**、四类设问每篇 ≥1 非细节、三篇中至少一篇**任务型/问答**（非纯单选）、真题母本改编 + `source_id` 溯源。
- 防越级 ≤ 语法上限；L1 综合诊断定位照旧。

## 三、交互（题型配比 + 菜单）

- **培优硬线**：全课选择题 **≤30%**、热区（语法/词汇/综合诊断）**≤25%**、纯识别 **≤35%**、全课主动作 **≥5 种**（点/拖/写/连/排/听）。
- 语法/词汇/随堂演练按 `interaction_menu_v1.json` 选模板；动笔提取与操作交互占主体；选择题只留"四选一辨析"合适处、每页 ≤2 道。
- 非选择交互**真实实现**（输入框/拖拽/连线/排序 + 判题 JS + saveAnswer 数据采集），**严禁只贴元数据标签**。
- 每题容器带互动合同元数据 + 页面 `<!-- CW-INTERACTION-CONTRACT:1 -->`。
- 阅读单选保留（真题对位），每 3 单选配 1 证据/短答。

## 四、翻页豁免（防点交互跳页）

- 文档级翻页监听豁免名单**必须包含** `.drag-container` / `.link-container` / `.order-container`（含子元素）；点交互元素不翻页、空白处翻页正常。

## 五、配套练习

- 对齐 exam_spec v2026.2（阅读30/语言25/综合25 + 教学诊断附件）；L4 缺失则一并生成；已有练习核验结构/溯源/答案分布（≤40%）、首行缩进 1 字符。

## 六、核验与验收

- L1–L5 各跑 `verify_v2.py --force` + `verify_interaction_v1.py --force`：**均 ERROR=0**。
- 培优硬线达标（两口径占比/动作种类）；档位 B 阅读达标；五课主色互异非红金。
- 浏览器实测：点 输入/拖拽/连线/排序 不翻页；判题正确；答错双击撤销。
- 已交付旧件（如 L1 旧版）归档 `_旧件归档_`，不覆盖删除历史。

## 七、回报（简短）

① 五课 核验/生成 状态表（每课 PASS/FAIL）② 变更文件清单 ③ 各课 选择占比/热区/识别/动作种类 实测 ④ 各课主色与变体 ⑤ 阅读（词数/生词率/任务型）实测 ⑥ 已知偏差。
