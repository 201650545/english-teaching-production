# 执行汇报 G2 · HUB 瘦身与同步治理（P2-1 → P2-3）

> 提交日期：2026-08-10　执行方：执行 Agent
> 依据：《GitHub三仓库架构优化方案_执行版》闸门制 G2
> 前置：G1 已确认通过（ETP 已推送 61a87fe..8efbdab）

## 【完成任务】

- **P2-1** 网关实例瘦身 —— **前提已不成立，无需代码动作**（见下）
- **P2-2** 实验残留与日志清理 —— `modelscope-daily/logs/` 移出跟踪
- **P2-3** feishu_sync 双写治理 —— 无交集，加独占声明 + TOPOLOGY.md 固化

## 【关键改动】文件级摘要

### P2-1 网关实例瘦身 → 前提核查结论
- **发现**：HUB 仓库（`D:\项目`，github.com/201650545/ai-hub）最新提交
  `48eac65`（此前已入库）**已删除 AI Hub 网关三件套**（01_网关模板 / 02_网关实例 /
  03_共享组件，47 文件 / 14545 删行），理由「Cherry Studio 已提供网关」。
- **现状核验**：仓库内 `01/03` 目录仅剩被忽略的 `__pycache__`；`02_网关实例/ds_v4_cli`
  仅剩被忽略的运行时数据（channels/history/quota/log）；**跟踪文件数 = 0**。
- 方案目标「共享模块在实例目录中不再存在拷贝」**已由 48eac65 达成**，本闸门零改动。
- 运行中的独立网关在仓库外 `D:\游戏\ds_v4_cli`（聚合端口 :3000），与 ai-hub 仓库无代码共享。

### P2-2 实验残留与日志清理（HUB）
- `.gitignore` 追加 `modelscope-daily/logs/`
- `git rm --cached` 移出跟踪 2 个日志（`2026-08-10.json`、`魔粒日报.md`），**本地文件保留**
- 验证：`git ls-files modelscope-daily/logs` = 0；本地 5 个日志文件仍在
- 注：方案列出的散装 JSON（filter1.json 等）随三件套删除已不在仓库内，无需迁移

### P2-3 feishu_sync 双写治理（HUB + ETP）
- 阅读两份脚本，**读写矩阵**（无交集）：

| 脚本 | 仓库 | 写入 Base/表 | 主键 |
|---|---|---|---|
| `00_工具/ops/feishu_sync.py` | ETP | 英语教学流水线 → 课程进度看板 `tblDQL47cLPeDkqg` | 课时编号 |
| `00_中央平台/feishu_sync.py` | HUB | AI Hub 网关数据 → gateways/api_channels/conversations/daily_stats 4 表 | name / gateway+channel 等 |

- 结论：**无交集** → 按方案 P2-3 §2 执行：两份脚本文件头加「独占写入」声明；
  新建 `D:\项目\TOPOLOGY.md` 固化双写分工（P4-1 将补齐完整拓扑）。

## 【回归/验证结果】

| 项目 | 结果 |
|---|---|
| P2-1 仓库内 01/02/03 跟踪文件 | 0 个 ✔（三件套已删） |
| P2-2 日志移出跟踪 | 跟踪 0 / 本地保留 ✔ |
| P2-3 读写矩阵 | 无交集 ✔（已加双端独占声明） |

## 【遗留与风险】需裁决事项

1. **P2-1 已先行完成**：三件套已被 `48eac65` 删除（Cherry Studio 替代网关）。
   **建议**：本闸门零代码动作；`01_网关模板/__pycache__` 等磁盘残留是否一并清理？
2. **HUB 根层杂散未跟踪目录**（非本次任务，需教师拍板去留）：
   - `feishu-data-hub/` —— FDH 仓库（Node 项目）的**完整副本**（含 node_modules），疑似误拷，且与 ai-hub 无引用关系；
   - `ai-resource-hub/` —— docs/README/方案书，疑似独立子项目；
   - `06_组件编排器/课时样例/E2EGEM/` —— html + png 样例。
3. **HUB push 待闸门**：fd6cd22（本次）+ 此前提交均未 push。HUB 工作区另有既有未提交修改
   （`魔粒守护.js` 等），属教师工作区，未触碰。
4. **modelscope-daily/logs 确认**：已移出跟踪但本地保留，符合「日志不再入库」。

## 【下一步】G3（P3-1 → P3-2 → P4-1）

- P3-1：ETP 新建 `.github/workflows/verify.yml`（JSON Schema 校验）；HUB 新建 `test.yml`（tests/run_all.py）
- P3-2：FDH 防噪音提交（内容 hash 对比）
- P4-1：TOPOLOGY.md 补齐 CI 工作流与三仓库服务关系

> 等待 G2 确认通过后进入 G3。
