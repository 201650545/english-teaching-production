# 许颖嘉 26 课 quiz-q 标签缺闭合修复 · 执行汇报（2026-08-07）

**执行方**：可读取 `D:\英语教学` 本地文档的 Agent（许颖嘉线）
**命令来源**：`D:\英语教学\00_总规划\许颖嘉26课quiz-q缺闭合修复命令_20260807.md`
**修复根因**：2026-08-06「属性泄露修复命令」替换模板 `\1 \2<div class="qq-text">` 中 `\2` 属性串以 `data-scorable="true"` 结尾且不含闭合 `>`，机械替换吞掉了 `quiz-q` 标签闭合 `>`，导致 `checkOpt(this)` 中 `var q = btn.parentNode` 取到 `.quiz-opts` 而非 `.quiz-q`，`q.dataset.qid` 为 undefined，`saveAnswer(...)` 分支不执行 → 答题数据不落库。

---

## 一、修复脚本

**脚本**：`D:\英语教学\00_工具\fix_quizq_close.py`（本次执行临时脚本，已保留至执行 Agent 工作目录，正文可复用）

**替换规则**（机械替换，只补闭合 `>`，不改任何属性值/题干/选项/答案/判题 JS/IndexedDB/page-id/页数/体积/契约标记）：

```text
旧：data-scorable="true"<div class="qq-text"
新：data-scorable="true"><div class="qq-text"
```

**范围**：仅 26 课成品文件 `课件成品_网页PPT\第XX课时_课件_基础.html`，跳过所有 `_旧件_*` 备份目录。

---

## 二、26 课每课替换处数统计

| 课时 | 替换处数 | 课时 | 替换处数 | 课时 | 替换处数 |
|---|---:|---|---:|---|---:|
| L01 | 63 | L10 | 63 | L19 | 63 |
| L02 | 63 | L11 | 63 | L20 | 63 |
| L03 | 63 | L12 | 63 | L21 | 63 |
| L04 | 63 | L13 | 63 | L22 | 63 |
| L05 | 63 | L14 | 63 | L23 | 63 |
| L06 | 63 | L15 | 63 | L24 | 63 |
| L07 | 63 | L16 | 63 | L25 | 63 |
| L08 | 63 | L17 | **73** | L26 | **37** |
| L09 | 63 | L18 | **13** | | |
| **合计** | | | | **1572** | |

**说明**：
- 23 课（L01–L16、L19–L25）各 63 处 = 1449；L17 = 73；L18 = 13（其余 63 处已由 L18 重做修复）；L26 = 37。合计 **1572**。
- 命令文件汇总写「1509」，为笔误（按命令文件表格：23×63 + 73 + 13 + 37 = 1572）。本汇报以实测 1572 为准。
- 修复后逐课核对：每个 `.quiz-q` 的 `data-scorable="true"` 后必须紧跟 `>` 再 `<div class="qq-text"`；26 课 quiz-q 总数 1635（23×63 + 73 + 76 + 37），全部正常闭合，无 `no_scorable`、无其他闭合异常。

---

## 三、「缺闭合 0」实测输出

**grep 验证**（成品 26 课，排除 `_旧件_`）：

```
模式 data-scorable="true"<div   → 0 处（成品26课全量为0）
模式 data-scorable="true"><div  → 与各课 quiz-q 数一致（L18 特殊：75 处 qq-text + 1 处 recall-grid）
```

**脚本全量核对**（26 课逐一解析 `<div class="quiz-q"` 开标签）：

```
TOTAL_QUIZQ=1635
TOTAL_MISSING_CLOSE=0        # 缺闭合 0
no_scorable=0（全部 quiz-q 均含 data-scorable）
other_missing=0（全部 quiz-q 均为 data-scorable="true"> 正常闭合）
```

> L18 说明：重做后 L18 为 76 个 quiz-q（75 个 `<div class="qq-text">` 结构 + 1 个 `recall-grid` 题型，后者无需 qq-text），均正常闭合。

---

## 四、verify 防复发（新增检查 3）

`D:\英语教学\00_工具\verify_interaction_v1.py` 的 `_check_attr_in_tag`（VIS-615）新增**检查 3**，原检查 1/2 保留：

```python
# 检查 3：quiz-q 标签必须闭合（data-scorable 后直接跟 <div 即缺 >）
missing = re.findall(r'data-scorable="true"<div class="qq-text"', html)
if missing:
    findings.append(Finding(
        "VIS-615", sev_error,
        f"quiz-q 标签缺闭合 >：检测到 {len(missing)} 处 'data-scorable=\"true\"<div' 模式（答题数据不落库，必须修复）"
    ))
```

**防复发能力单元验证**（构造用例直接调用 `_check_attr_in_tag`）：
- 缺闭合 1 处 → 触发 1 条 ERROR（"quiz-q 标签缺闭合 >：检测到 1 处…"）✓
- 正常闭合 → 0 条 ERROR ✓
- 缺闭合 60 处 → 聚合 1 条 ERROR（"检测到 60 处…"）✓

---

## 五、双验证器回归（26 课全量，--force）

### 5.1 verify_v2.py --force

| 指标 | 结果 |
|---|---|
| PASS | **25/26** |
| FAIL | **1/26 — L22**（既有基线，非本次引入） |

L22 FAIL 原因：`verify_v2` 的 RESIDUALS 名单含 `L22_`，而 L22 的 63 处 `L22_` 全部是合法 `data-question-id="XYJ_L22_LX22_QNN"` 前缀，被误判为残留框架。修复前旧件（`_旧件_差异化20260806\`）同样 FAIL（63 处），2026-08-06 批量补交互汇报已记录此既有状态。

### 5.2 verify_interaction_v1.py --force

| 指标 | 结果 |
|---|---|
| VIS-615（含新增检查 3）ERROR | **26/26 全为 0** ✅ |
| 其他 ERROR（VIS-601/602/603/604） | 25 课各 8 条 / L26 7 条 / L18 0 条——**既有基线，非本次引入** |

既有基线证明：
- 对比修复前备份（`_旧件_差异化B层补足20260807`，L09/L10/L11/L12/L17/L26 六课）：修复前后非 VIS-615 ERROR 数量完全一致（8/8/8/8/8/7），无新增 ERROR。
- 2026-08-07 差异化 B 层补足汇报已记录：VIS-601/602/603 为 A 层 100% 选择设计的既有基线，非回归。
- VIS-615：修复前 1 条 ERROR（缺闭合/泄露）→ 修复后 0 条，本次修复消除了该 ERROR。

### 5.3 页面无 `data-*` 代码串显示

修复后 `data-scorable="true"<div` 缺闭合特征 0 处，属性串不再被当作正文渲染（原有检查 1/2 的 `> data-interaction-item=` 泄露模式在 26 课成品中亦为 0 处）。

---

## 六、浏览器实测（已知偏差）

**状态**：跳过，环境受限。
**原因**：执行环境无标准路径 Chrome/Edge（`C:\Program Files\Google\Chrome\Application\chrome.exe` 等不存在）、`agent-browser` 未安装、npm prefix 指向不存在的目录。与 2026-08-07 差异化 B 层补足执行时相同的环境限制。
**补偿**：以结构层双验证器 + 逐课 `quiz-q` 开标签全量解析替代。缺闭合修复为纯机械补 `>`，不触碰判题逻辑与采集链路（`checkOpt`/`saveAnswer`/IndexedDB 代码零改动）。
**待办**：收敛方/有浏览器环境复核 L09 + L26 答题后 IndexedDB `answerRecords` 有记录、导出答题数据非空。

---

## 七、变更清单

| 项 | 变更 |
|---|---|
| 26 课成品 HTML | 每课补 `data-scorable="true"` 后缺失的闭合 `>`（合计 1572 处），仅此一项 |
| `00_工具\verify_interaction_v1.py` | `_check_attr_in_tag` 新增检查 3（缺闭合检测），检查 1/2 保留 |
| `_旧件_*` 备份目录 | 未动 |

**可恢复**：如需回退，从 `_旧件_差异化20260806\`（或对应最新旧件）覆盖回成品目录即可，但会同时回退差异化/B 层/属性修复等其他变更，不建议。

---

> 汇报完毕，已写入 `00_总规划\05_交付与审核记录\许颖嘉26课quiz-q缺闭合修复_执行汇报_20260807.md`。无需另行通知，收口方定时读取。
