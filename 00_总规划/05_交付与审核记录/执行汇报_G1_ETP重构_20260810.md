# 执行汇报 G1 · ETP 重构（P1-0 → P1-3）

> 提交日期：2026-08-10　执行方：执行 Agent
> 依据：《GitHub三仓库架构优化方案_执行版》闸门制 G1
> 前置：G0 隐私处理已确认通过（仓库转私有 + 范围A仅洗展示层 + FDH 姓名黑名单）

## 【完成任务】

- **P1-0** 回归基线（静态 hash 基线，不入仓库）
- **P1-1** 一次性脚本收口（统一入口 + 归档 31 脚本 + 红线）
- **P1-2** 代码与数据分离（engine/build/ops + `01_数据/` + Schema 校验）
- **P1-3** 命令文档归档（根层 37 → 7，索引一致）

## 【关键改动】文件级摘要

### P1-0 静态基线（G1 前置）
- 基线落盘 `C:\Users\郭永涛\AppData\Local\Temp\p1_0_baseline\`（BASELINE.md + baseline_manifest.json）
- 结论：DXH L06 练习 DOCX **字节级可复现**（`random.Random(lesson)` 种子），为后续回归提供确定性基准
- 教训：禁止在测试中 import 题库构建器（曾触发 passage_bank 再生 376 删行，已还原）

### P1-1 脚本收口
- **新增** `00_工具/build/build_practice.py` —— 统一练习入口，只做「加载 `{card,content}` 已拼装 JSON + 调既有引擎」，不复制业务逻辑
- **移动** 31 个一生一课脚本 → `00_工具/archive_scripts/`（保留可运行，附 README 说明 HERE 路径调整）
- **豁免** `gen_l1_l13_v2.py` 因被 `courseware_engine.py` 依赖（L4 复习数据源）移回 `engine/`
- **红线** `00_格式规范/00_全局约束与红线.md` 新增条款 7：禁止新增按学生/课时命名的一次性构建脚本

### P1-2 代码与数据分离
- **新增** `00_工具/validate_banks.py` —— 无依赖 JSON Schema 校验器（必填 + 类型）
- **新增** `01_数据/schemas/` 7 个题库 Schema（draft-07）
- **移动** 11 个引擎 → `00_工具/engine/`；统一入口 → `build/`；`publish_all.py`/`feishu_sync.py` → `ops/`
- **移动** 数据 JSON → `01_数据/{content(23), banks(7), schemas(12)}`
- 验收：`00_工具/` 无数据 `.json`；validate_banks 全量通过

### P1-3 命令文档归档
- **移动** 29 件已交付命令文档 → `00_总规划/06_归档/命令归档/`（该目录被 publish_all 顶层排除，不入库）
- **归位** 错位汇报 `邓兴华L10-12修复执行汇报` → `05_交付与审核记录/`
- **重写** `00_总规划/README_索引.md`（顶层只列 3 个活跃文档）
- **新增** `06_归档/命令归档/README.md`（归档清单，代号化）
- 教师裁决追加归档 4 件无专属汇报命令（L07L08 重构 / L07L09 翻页修复 / 封面与顶栏 / 验证器阶段一）→ 归档共 **33 件**
- 验收：根层 37 → **3**（≤10 ✔）；索引与实际一致；`publish_all --dry` = `(无变更)`

## 【回归结果】

| 项目 | 结果 |
|---|---|
| P1-0 静态基线 | 3+ 份 hash 清单落盘 ✔ |
| 统一入口复现（DXH L06） | document.xml `080d6f9c02bb` 与旧脚本产物一致 ✔ |
| validate_banks.py（7 题库） | 全部通过 ✔ |
| publish_all --dry（P1-3 后） | `(无变更)`，归档不回流 ✔ |

## 【遗留与风险】需裁决事项

1. **ETP push 待闸门**：P0-1→P1-3 共 6 个本地提交（61a87fe / baaa76a / fd4b5db / 2cdc3a7 / acacbd0 / de5ccd5）未 push，G1 确认后推送。
2. ~~根层保留 4 个命令文档~~ **已裁决**：教师 2026-08-10 拍板一并归档 → `06_归档/命令归档/`，根层最终 3 个（规范/索引/方案）。
3. **FDH push 挂起**：提交 20e92b0（安全扫描黑名单）未 push，缺 GitHub OAuth `workflow` scope。教师联网执行 `! gh auth refresh -h github.com -s workflow` 后即可推。
4. **姓名残留**：运营文档（05 汇报/命令文档）文件名与正文保留真实姓名——G0 范围A 批准（仅洗展示层）+ 仓库已私有，一致。

## 【下一步】G2（P2-1 → P2-3，HUB）

- P2-1 网关实例瘦身：`03_共享组件/` 升级为包，先迁 `my_hub` 再迁 `ds_v4_cli`，`tests/run_all.py` 回归
- P2-2 实验残留清理：散装 JSON → `_scratch/`，日志 `git rm --cached`
- P2-3 feishu_sync 双写治理：产出「脚本 → 飞书表」读写矩阵，无交集则注释声明 + TOPOLOGY 固化

> 等待 G1 确认通过后进入 G2。
