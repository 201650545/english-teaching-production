# 许颖嘉 26 课 checkOpt 采集修复 · 执行汇报（2026-08-08）

> 依据命令：`00_总规划\许颖嘉26课checkOpt采集修复命令_20260808.md`
> 执行根因：`checkOpt` 用 `btn.parentNode` 取题容器，拿到的是 `.quiz-opts`（无 `data-qid`），导致 `saveAnswer` 分支不执行、答题数据永不落库。

---

## ① 26 课替换统计（checkOpt → closest 写法）

每课成品路径：`许颖嘉\第XX课时\课件成品_网页PPT\第XX课时_课件_基础.html`

- 替换规则：`var q=btn.parentNode;` → `var q=btn.closest('.quiz-q')||btn.parentNode;`
- 26 课**每课恰好 1 个 `function checkOpt`、1 处旧写法**，逐课各替换 1 处，**合计 26 处**。
- 均未发现第二处 `function checkOpt` 残留（L18 新题型函数 `checkFill/checkDrag/checkOrder/checkFlip` 本就使用 `closest('.quiz-q')`，仅其保留的旧单选 `checkOpt` 需替换，同样 1 处）。
- 只改了这一行定位，未触碰判题/高亮/气泡/双击撤销/IndexedDB 逻辑，未改题干/选项/答案/CSS/结构。

| 课时 | 替换处数 | 课时 | 替换处数 | 课时 | 替换处数 |
|---|---|---|---|---|---|
| 01 | 1 | 10 | 1 | 19 | 1 |
| 02 | 1 | 11 | 1 | 20 | 1 |
| 03 | 1 | 12 | 1 | 21 | 1 |
| 04 | 1 | 13 | 1 | 22 | 1 |
| 05 | 1 | 14 | 1 | 23 | 1 |
| 06 | 1 | 15 | 1 | 24 | 1 |
| 07 | 1 | 16 | 1 | 25 | 1 |
| 08 | 1 | 17 | 1 | 26 | 1 |
| 09 | 1 | 18 | 1 | **合计** | **26** |

替换后示例（L09）：
```js
function checkOpt(btn){
  var q=btn.closest('.quiz-q')||btn.parentNode; if(q.dataset.done) return; q.dataset.done='1';
  ...
}
```

---

## ② 「btn.parentNode→closest」全量 0 残留实测

grep / 脚本逐课扫描（26 课成品主文件）：

- `var q=btn.parentNode`（旧写法，含 `var q=` 前缀）：**26 课共 0 处**。
- `closest('.quiz-q')`：**每课 ≥2 处**（L01–L17/L19–L26 各 2 处；L18 因新题型多函数共用 closest 为 7 处），满足每课 ≥1。
- 每课 `checkOpt` 函数体 1 个，函数体内旧写法命中 **0 处**。

结论：**btn.parentNode → closest 全量 0 残留，无遗漏。**

---

## ③ 双验证器回归（含 VIS-616）

### verify_interaction_v1.py --force（含新增 VIS-616 结构校验）

- **VIS-616（新增）**：26 课 **0 ERROR**。所有 `.quiz-opt` / `.fill-input` / `.drag-word` 均能向上到达 `.quiz-q` 容器，无游离按钮。
- **单元验证**：
  - 正常课（26 课全量）：VIS-616 0 ERROR。
  - 人为把 `quiz-opt` 移出 `quiz-q`、外加游离 `fill-input` 的构造 HTML：正确触发 `ERROR VIS-616`（quiz-opt 1/3、fill-input 1/1 游离）。容器内按钮不误报。
- **说明**：`--force` 下绝大部分课仍报出**既有**的 VIS-601/602/603/604/606 选题占比 / 动作种类偏少错误（这些课为全选择题，属设计口径问题，非本次改动引入）。仅 **L18（混合题型）0 ERROR**、L26 为 7 个。**反向 A/B 实证**：把当前 L09 的 closest 行临时还原为旧 `btn.parentNode` 写法，错误数完全一致（8→8、无任何差异），证明 **checkOpt 改动零新增错误、零回退**。

### verify_v2.py --force

- **25/26 PASS**；**L22 FAIL**。
- L22 失败原因：残留框架 `L22_` ×63（verify_v2 硬规则「残留框架」拒收）。该残留为成品制作阶段引入，**早于本次 checkOpt 改动**（备份 `_旧件_补交互20260806` 中 `L22_` 计数为 0、当前主文件为 63），且与 checkOpt 定位行无关，**非本命令回退**。
- 所有课 `JS检查`（node --check）通过，证明 closest 改动 JS 语法合法。

---

## ④ 浏览器实测 / IndexedDB 记录数

**跳过（已知偏差）**：本环境无 Chrome / agent-browser，无法做 CDP 答题实测（L09/L26 单选、L18 单选记录数核实、双击撤销恢复等）。按命令 §三.4/5/6 记为已知偏差，留待收口方在有浏览器环境实测验证。

---

## ⑤ 已知偏差汇总

1. **L22 verify_v2 FAIL**：残留框架 `L22_`×63，成品制作期遗留，早于本命令、与 checkOpt 无关，非回退。
2. **verify_interaction --force 大部分课报既有 VIS-601/602/603/604/606**：全选择题设计口径问题，非本次改动引入（A/B 反向实证零差异）。
3. **浏览器实测（CDP 答题 / IndexedDB / 双击撤销）未执行**：环境无 Chrome/agent-browser。
4. `node` 依赖：verify_v2 需 `node --check`，本机已通过 `D:\Program Files\nodejs` 注入 PATH 正常运行。

---

## 结论

- 26 课 checkOpt 全部改为 `closest('.quiz-q')||btn.parentNode`，每课 1 处、共 26 处，`btn.parentNode` 旧写法全量 0 残留。
- 新增 VIS-616 结构校验 26 课 0 ERROR，单元验证通过（游离按钮正确触发、正常课不误报）。
- checkOpt 改动**零新增错误、零回退**（verify_v2 除既有 L22 残留外全 PASS；verify_interaction 除既有设计口径错误外无新增）。
- 采集断裂根因（容器定位错位→`saveAnswer` 不落库）已从代码层面消除，待浏览器实测确认落库。