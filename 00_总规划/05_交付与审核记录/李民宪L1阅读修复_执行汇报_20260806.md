# 李民宪 L1 阅读修复 · 执行汇报（2026-08-06）

## ① 改动文件清单

- `李民宪\第01课时\课件成品_网页PPT\第01课时_课件_培优.html`（唯一改动文件）
- 旧版已归档至 `李民宪\第01课时\_旧件_20260806\`

---

## ② 三篇阅读实测

| 篇目 | 段落数 | 词数 | 设问类型 | 是否符合档位 B 基线 |
|------|--------|------|----------|---------------------|
| A 篇（My Wonderful Weekend） | 3 个 `<p>` | 143 词 | 细节/推断/词汇/主旨 | ✅ |
| B 篇（Why Holidays Are Good for You） | 4 个 `<p>` | 142 词 | 细节/词义/推断/主旨 | ✅ |
| C 篇（How to Stay Safe on Holiday） | 3 个 `<p>` | 156 词 | 回答问题 x4（任务型） | ✅ |

**C 篇改写要点**：原列表式安全提示改写为连贯语篇，加入 2 处 `because` 从句（"because safety comes first" / "because that can make you sick"）、2 处 `if` 条件状语（"If the weather is bad…" / "If you meet a little trouble…"）、1 处 `when` 时间状语（"When you are on the trip…"）。原 5 条要点改为 3 段递进结构（行前准备→途中安全→应急处理）。

**source_id 残留**：3 处 `{{source_id:...}}` 明文全部清除，改为 `data-source-id` 属性。

---

## ③ 答案分布

修复前：A3 / B4 / C2（B=44% ❌）
修复后：A3 / B3 / C2（最大占比 38% ✅，≤40%）

措施：Q101 从选择题转写为 fill 写作题，减少选择总数到 8 题。

---

## ④ 双 --force 输出

### verify_v2 --force：PASS ✅

```
答案分布: {'A': 3, 'B': 3, 'C': 2} 最大占比38%(≤40%) ✓
答案可判: quiz-opt / 8 题 ✓
JS检查: ✓
div平衡: 1779/1779 ✓
防越级: 未命中 ✓
```

### verify_interaction_v1 --force --level advanced：PASS ✅（ERROR=0）

新增交互容器元数据：
- P16 句型分类天平 → drag_and_drop / drag（grammar）
- P18 过去式翻牌 → flip_card / flip（grammar）
- P11 词块合成器 → assembly / assemble（vocab）
- P21 不定代词配对 → link / link（drill）
- Q99 句型判断 → classify（diagnosis）

动作种类：write / select / link / drag / flip / assemble / classify = **7 种**（≥5 ✅）

各环节动作种类：
| 环节 | 动作种类 | 判定 |
|------|----------|------|
| vocab | write + assemble = 2 | ✅ |
| grammar | write + drag + flip = 3 | ✅ |
| drill | write + link = 2 | ✅ |
| diagnosis | write + classify = 2 | ✅ |

---

## ⑤ 已知偏差

- 视觉软检查（字号过小 HIGH-WARN × 118、CSS 重复选择器/孤儿选择器/颜色字面量 WARN × 143）为 L1 原始课件内置，非本次修复引入，不阻断交付。
- `will×1` 命中的防越级扫描经人工复核为 C 篇改写中 "you will" 的促成表达，非将来时语法教学，属合法使用。