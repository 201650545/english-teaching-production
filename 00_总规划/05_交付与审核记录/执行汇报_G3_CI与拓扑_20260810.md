# 执行汇报 G3 · CI 接入与拓扑（P3-1 → P3-2 → P4-1）

> 提交日期：2026-08-10　执行方：执行 Agent
> 依据：《GitHub三仓库架构优化方案_执行版》闸门制 G3
> 前置：G2 已确认通过（HUB/ETP 已推送）

## 【完成任务】

- **P3-1** CI 接入（ETP verify.yml + HUB test.yml）
- **P3-2** FDH 防噪音部署（内容 hash 对比跳过 Pages 部署）
- **P4-1** TOPOLOGY.md（三仓库关系 + CI 拓扑 + 双写分工）

## 【关键改动】文件级摘要

### P3-1 CI 接入
- **ETP** 新增 `.github/workflows/verify.yml`（push/PR 触发）：
  `validate_banks.py`（题库 Schema）+ `validate_content.py`（内容 JSON 有效性）
- **HUB** 新增 `.github/workflows/test.yml`（push/PR 触发）：`python tests/run_all.py`
- **HUB 测试套件修复**：`tests/run_all.py` 改为 importlib 动态加载，缺失共享组件
  模块的套件（网关/引擎/历史/额度）优雅 SKIP。本地回归 **32/36 通过 / 0 失败 / 4 跳过**
- **数据修复**：`01_数据/content/practice_content_LMX_L01.json` 存在前导杂散字节
  （非合法 JSON）→ 已去除 1 字节修复（源 + staging）

### P3-2 FDH 防噪音部署
- `scripts/sync-hub.mjs`：新增实质内容 sha256 计算（排除 `catalog-versioned/` 目录
  与 build_id 字符串）→ 写入 `public/.content-hash`；与上次部署 hash
  （`FDH_PREV_HASH_URL` 抓取）一致时写 `public/.deploy-skip` 标记
- `sync-daily.yml` / `sync-hourly.yml`：检测 `.deploy-skip` → 跳过
  validate/security/upload/deploy；未配置 URL 或 `--force` 时回退正常发布
- 逻辑单测通过：同内容不同 build_id hash 相等 ✔；内容变化 hash 变化 ✔

### P4-1 拓扑固化
- `D:\项目\TOPOLOGY.md`：三仓库关系、CI 工作流拓扑、飞书双写分工、网关拓扑
- ETP 镜像副本 → `00_总规划/04_方案与规划/TOPOLOGY.md`

## 【回归/验证结果】

| 项目 | 结果 |
|---|---|
| ETP validate_content.py | 全部通过 ✔（含修复后的 LMX_L01） |
| ETP validate_banks.py | 全部通过 ✔ |
| HUB tests/run_all.py | 32/36 通过 / 0 失败 / 4 跳过 ✔ |
| HUB run_all.py `npm run check` 类比 | node --check 通过 ✔ |
| FDH npm run check | 全部通过 ✔ |
| FDH 防噪音 hash 单测 | build_id 无关 ✔ / 内容敏感 ✔ |

## 【遗留与风险】需裁决事项

1. **FDH push 仍挂起**：3 个本地提交（20e92b0 G0 安全扫描 / 8b72713 P3-2 防噪音）
   未推送。缺 GitHub OAuth `workflow` scope。教师联网执行
   `! gh auth refresh -h github.com -s workflow` 后即可推。
2. **新 workflow 待首次触发**：ETP verify.yml 与 HUB test.yml 需各触发一次验证
   （下一次 push 即自动触发）。HUB 本地回归已证实可通过，ETP 校验器本地全过。
3. **需设置的 Secrets**：
   - `FDH_NAME_BLACKLIST`（G0）：三位学生真实姓名，逗号分隔 → 激活 CI 姓名黑名单
   - `FDH_PREV_HASH_URL`（P3-2）：上次部署 `.content-hash` 的 URL
     （如 `https://<user>.github.io/<repo>/.content-hash`）；不设则防噪音不生效（回退正常发布）
4. **HUB/ETP 本次提交未 push**（31e9bc6 等），G3 确认后推送。
5. **LMX_L01 修复说明**：仅去除 1 个前导杂散字节，内容未改动；如属有意为之请告知。

## 【下一步】

G3 为最后一个闸门。确认后：推送 HUB/ETP，完成全部执行；方案中「明确不做的事」
（不改引擎/样式/飞书表结构/不清历史/不合并仓库）均未触碰。

> 等待 G3 确认。
