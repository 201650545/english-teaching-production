# 邓兴华 L08 · 视觉与结构创新命令（2026-08-06）

**执行方**：可读取 `D:\英语教学` 本地文档的 Agent（邓兴华线）
**缘由（教师反馈 2026-08-06）**：
1. L08 当前主色 teal 绿（`--brand:#0F766E`）+ 橙（`--accent:#F97316`），教师嫌**难看/丑**；
2. 契约原本指定天蓝 `#3498DB` + 暖黄 `#F39C12`，成品未按契约配色；
3. 结构可同步创新，按知识点自选风格（依据 `09_风格包规范.md` V2.0）。

---

## 〇、现状

文件：`邓兴华\第08课时\课件成品_网页PPT\第08课时_课件_中等.html`（43 页，163,456B）
知识点：一般过去时（was/were & did）与时间状语；主题语义场=**过去时·旅行经历**；阅读主题 Getting Dressed for Yesterday's Trip / Yesterday's Trip to the Beach；Stage 3（L8–L12）授课课。
契约参考：`契约\1_课程概要.md`（品牌主色天蓝 `#3498DB` / 强调暖黄 `#F39C12`，未强制）。

## 一、视觉重设计（教师核心诉求）

### 1. 换掉 teal 绿主色（必改）
- 弃用 `#0F766E` / `#134E4A` / `#86EFAC` / `#99F6E4` 等绿系为**主体主色**。
- **避开色**：大面积绿、刺眼黄/橙（教师已两次反馈绿/黄丑）、以及 L09 已用的**靛蓝/藏蓝 `#4F46E5/#1D4ED8`**（相邻课不撞色）。
- 新主色由你根据 L08 知识点自选，方向参考（**过去时·旅行经历** → 回忆/日志/旧物语义，可选其一，不强求）：
  - **复古旅行日志/旧照片风**：深棕 `#6B4F2E` + 米白 `#F5EFE3` + 卡其，老式行李箱/护照/邮票/拍立得元素，呼应"Yesterday's Trip"；
  - **旧报纸/档案风**：砖红 `#A63D2F` + 米黄纸色 `#F2E9D8`，报纸头条/时间线元素，呼应"时间状语"；
  - **夜间航行/星图风**：深藏青 `#1B2A4A` + 暖金点缀（金仅作强调、非主体），航海日志/星轨元素。
- 整课主色统一、可用渐变/纹理，但**视觉主体不再是大面积绿，也非大片黄/橙**。

### 2. 结构创新（允许打破默认封面/页面布局）
- 封面：不强制 `cover-badge→cover-title→cover-meta` 固定顺序；可根据主题重排（如旅行日志扉页式、报纸头条式、航线图式），信息标签位置自定，**但信息完整**（课次/知识点/时长/页数）。
- 页面：允许不同于其他课件的版式（双栏/卡片/时间线/地图路线图等），只要：
  - 保留 `class="page" id="pageN"` 契约（翻页/答题机制可用）；
  - 保留题容器与判题 JS、数据采集（IndexedDB）；
  - 每页可视区不拥挤、正文 ≥14px。
- 鼓励创意：组件变体、进入动画、主题图标均可自由发挥，**只要知识点与交互真实**。

### 3. 按 09 规范 V2.0 声明风格合同（新增）
在 HTML 中按 `09_风格包规范.md` V2.0 加入机器可读 `style_manifest`（JSON 注释或 script 均可），声明三轴：
```json
{
  "course_id": "DXH_L08",
  "student_level": "STANDARD",
  "lesson_mode": "TEACH",
  "style_family": "S04或S07或S05（自定，对应你选的方向）",
  "style_vars": {
    "layout_archetype": "时间线/日志/档案（自定）",
    "density_level": "medium",
    "type_profile": "自定",
    "surface_profile": "自定",
    "palette_profile": "自定（非绿非黄非靛蓝主体）",
    "media_language": "自定",
    "motion_profile": "自定",
    "feedback_profile": "自定"
  },
  "difference_from_recent_lessons": ["与L09靛蓝区分", "弃用teal绿", "（其他创新点）"]
}
```

## 二、验证器口径（视觉层放宽）

- **硬规则保留**（必须满足，无法放开）：
  - 体积 ≥150KB、页数 40-45；
  - div 开合平衡、无脚本残留、JS `node --check` 通过；
  - 每题唯一 `data-correct`、答案分布 ≤40%；
  - `CW-INTERACTION-CONTRACT:1` + 题容器元数据；IndexedDB 数据采集可用；
  - 交互真实（VIS-613 DOM 实测，严禁只贴元数据换皮）。
- **视觉层放宽**：verify_v2 视觉软检查（字号/颜色/圆角等 WARN、CSS_EXTRA 标记、cover 类名固定顺序）**不作为交付否决项**；视觉 ERROR 若来自"创新结构打破默认类名"，回报中书面说明理由后放行。
- 目标：**ERROR=0 尽量满足，创新导致的视觉类 WARN/ERROR 以书面理由放行**。

## 三、红线

- 不改知识点、题干事实、正确答案、阅读原文与正式原题；
- 交互必须真实实现（真 UI + 判题 JS + saveAnswer），**严禁只贴元数据标签换皮**；
- 不新增第三方库 / 第二数据库 / 联网依赖；
- 旧版 L08 归档 `_旧件_20260806\`，不覆盖删除历史。

## 四、验收

1. `verify_v2.py --force`：硬规则项（体积/页数/div/答案分布/合同）ERROR=0；视觉层偏离项已书面说明；
2. `verify_interaction_v1.py --force` + VIS-613 DOM 实测：交互真实；
3. 浏览器实测：翻页/答题/数据采集正常；封面信息完整；**新主色无大面积绿/黄/橙、与 L09 靛蓝可区分**；
4. 回报中附：新主色值、封面/页面结构说明、`style_manifest`、视觉层放行项清单及理由。

## 五、回报（简短 + 汇报落盘）

① 改动文件清单 ② 新主色值 + 封面/页面结构设计说明 + `style_manifest` ③ 验证器放行项及书面理由 ④ 双--force 输出 ⑤ 浏览器实测 ⑥ 已知偏差。

> **汇报落盘**：完成后写入 `D:\英语教学\00_总规划\05_交付与审核记录\邓兴华L08视觉创新_执行汇报_20260806.md`，无需另行通知。
