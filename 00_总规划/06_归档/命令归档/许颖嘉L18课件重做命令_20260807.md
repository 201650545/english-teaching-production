# 许颖嘉 L18 · 课件重做命令（2026-08-07）

**执行方**：可读取 `D:\英语教学` 本地文档的 Agent（许颖嘉线）
**缘由（教师反馈 + 收口实测 2026-08-07）**：
1. 教师反馈 L18 选择题题目上方显示 HTML 代码（`data-interaction-*` 属性泄露）——已由 `许颖嘉26课补交互属性泄露修复命令` 修复（26 课全清）；
2. 实测 L18 仍有 **选择题占比 100%**（75/75 全单选）+ 动作种类仅 1 种——**旧件固有**，教师决策：**现在重做 L18**，补多样题型 + 重审题目。
**依据**：`许颖嘉\内容蓝图\单课\第18课_内容蓝图.md`（现在进行时·家务与日常活动，G46 进行时结构 / G47 V-ing 变化 / G48 标志词）+ `04_分级差异化参数表.md`（基础档）。

---

## 〇、现状

| 项 | 现状 | 目标 |
|---|---|---|
| 课名 | 现在进行时·家务与日常活动 | 不变 |
| 选择占比 | 100%（75/75 全单选） | 基础档全课 ≤55%、热区 ≤45% |
| 动作种类 | 1 种（select） | ≥5 种（点/拖/写/连/排/听） |
| 交互合同/元数据 | 有 | 保持 |
| 页数/体积 | 43 页 / 220KB | 40-45 / ≥150KB 保持 |
| 配套练习 | 已有 | 不动 |

## 一、重做要求

### 1. 多样题型（核心，选择题占比压下来）
把低认知层级选择题**转写为操作/生成类**，保留正式阅读单选与必要的四选一辨析（每页 ≤2 道）：
- **填空**（fill_in）：家务动词变形填空（sweep→sweeping）、进行时结构填空（is/am/are + V-ing）；
- **排序**（order）：V-ing 变化规则步骤排序（加-ing→去e加-ing→双写）、句子成分排序；
- **连线/配对**（link）：主语与 be 动词配对、标志词（now/Look!/Listen!）与进行时配对；
- **拖拽分类**（drag）：V-ing 三种变化规则分类（直接加/去e加/双写）；
- **翻牌**（flip）：现在进行时标志词识别、家务动词识图；
- **拼写**（write）：根据中文写 V-ing 形式。

转写后：选择题容器占比（DOM 口径）基础档 ≤55%、热区 ≤45%；动作种类 ≥5。

### 2. 题目重审（教师发现题目问题）
- 逐题核对题干/选项/答案是否与 L18 知识点（现在进行时·家务）匹配；
- **page3「上节课复习」、page4「上节课词汇」**（L17 频度副词复习）——保留作为复习环节，但**明确页面标注"上节课复习"**，不让教师误认为本课新题；
- 纠正任何题干事实错误、选项冗余、答案标记错误。

### 3. 结构/视觉保持
- 保留 page-id 契约、交互合同 `CW-INTERACTION-CONTRACT:1`、题容器元数据（在标签内！）；
- 保留 A 层差异化换肤（`<CW-DIFF-STYLE>` 标记）与 B 层深化；
- 正误反馈语义色保留（配文字/图标，不靠颜色）。

## 二、红线

- 不改知识点框架、阅读原文、正式原题；
- 可重写/转写**练习类题目**（填空/排序/连线/拖拽等）以达题型多样，但**不得改变**正确语法答案；
- 不新增第三方库 / 联网依赖；
- 备份到 `课件成品_网页PPT\_旧件_L18重做20260807\`。

## 三、验收

1. `verify_v2.py --force`：ERROR=0（硬规则）+ 体积 ≥150KB + 页数 40-45；
2. `verify_interaction_v1.py --force`：无 ERROR；选择题占比 ≤55%（全课）/≤45%（热区）；动作 ≥5 种；
3. 浏览器实测：各题型真实可操作（填空 checkFill/排序 order/连线 link/拖拽 drag）、翻页/双击撤销/IndexedDB 正常、**页面不再显示 data-* 代码**；
4. 页面抽查：复习环节明确标注、题目与知识点匹配。

## 四、回报（简短 + 汇报落盘）

① 改动明细（转写了哪些题/新增哪些题型/题目修正清单）② 重做后选择占比与动作种类实测 ③ 双验证器输出 ④ 浏览器实测 ⑤ 已知偏差。

> **汇报落盘（必读）**：完成后将回报写入 **`D:\英语教学\00_总规划\05_交付与审核记录\许颖嘉L18重做_执行汇报_20260807.md`**（不存在则新建，已存在则覆盖为最新）。写好后**无需另行通知**，收口方定时读取该文件。
