# 许颖嘉 B 层六课多样题型命令（2026-08-08）

**执行方**：可读取 `D:\英语教学` 本地文档的 Agent（许颖嘉线）
**教师决策（2026-08-07）**：B 层六课（L09/10/11/12/17/26）选择占比仍为 A 层 100% 基线，**六课都补多样题型**（L18 已示范：单选 100%→17%）。
**前置已完成**：quiz-q 缺闭合修复（08-07）+ checkOpt 采集修复（08-08）均已闭环，26 课采集正常。本命令在**修复后的文件**上转写题型，不得回退修复成果。
**依据**：`许颖嘉26课checkOpt采集修复命令_20260808.md` 后的当前成品 + 各课内容蓝图。

---

## 〇、现状（收口实测）

| 课时 | 题数 | 单选 | 占比 | 动作 | 主题（蓝图） |
|---|---|---|---|---|---|
| L09 | 63 | 63 | 100% | 1 | 问路与方向 |
| L10 | 63 | 63 | 100% | 1 | 待上（学科） |
| L11 | 63 | 63 | 100% | 1 | 待上 |
| L12 | 63 | 63 | 100% | 1 | 点餐 |
| L17 | 73 | 73 | 100% | 1 | 习惯 |
| L26 | 37 | 37 | 100% | 1 | 写作 SOP |

## 一、转写要求（六课）

**参考 L18 重做模式**（`许颖嘉L18课件重做命令_20260807.md` 已示范达标：单选 17%、动作 7 种）：

1. 把低认知层级单选转写为**操作/生成类**，保留正式阅读单选与必要的四选一辨析（每页 ≤2 道）：
   - **填空 fill_in**（checkFill）：语法定点填空（L09 方位介词填空 / L17 频度副词变形 / L26 写作句法填空）；
   - **排序 order**：句序/步骤排序（L10 学科学习步骤 / L26 SOP 步骤排序）；
   - **连线/配对 link**：主谓配对（L09 方位介词与方向 / L12 点餐用语配对 / L17 习惯与时间副词）；
   - **拖拽分类 drag**：分类归纳（L12 食物分类 / L17 健康习惯分类）；
   - **翻牌 flip**：词汇/语法点识别（各课新词）；
   - **写作 write**：短答/仿写（L26 写作 SOP 核心）。
2. **转写后目标**（基础档 DOM 口径）：全课选择题占比 ≤55%、热区 ≤45%；动作种类 ≥5；
3. **判题函数用新引擎写法**：`checkFill/checkOrder/checkLink/checkDrag/checkFlip` 等一律 `closest('.quiz-q')||btn.parentNode` 容器定位 + IndexedDB 采集 + 双击撤销——**禁止退回旧 parentNode 写法**（采集已修复，不得回退）；
4. 交互元数据完整：每个题容器 `data-interaction-item/data-question-id/data-knowledge-id/data-section/data-template-id/data-interaction-type/data-action-type/data-cognitive-level/data-scorable` **在标签内**（缺闭合修复已做，不得回退成标签外）；
5. 正误反馈语义色保留（配文字/图标，不靠颜色）。

## 二、红线

- **不改**知识点框架、阅读原文、正式原题；可重写/转写**练习类题目**以达题型多样，但不得改变正确语法答案；
- **L14-21 无过去时约束对许颖嘉不适用**（那是邓兴华蓝图的红线），但**不得越级**（不超本课语法上限）；
- 保持 page-id 契约、页数 40-45、体积 ≥150KB、交互合同 `CW-INTERACTION-CONTRACT:1`、答案分布 ≤40%、A 层/B 层差异化标记保留；
- 不新增第三方库/联网依赖；不改判题 JS 现有逻辑（只按新写法扩展新题型函数）；
- 改动前每课备份到 `课件成品_网页PPT\_旧件_多样题型20260808\`。

## 三、验收

1. 六课选择占比（DOM 口径）全课 ≤55%、热区 ≤45%；动作种类 ≥5；
2. `verify_v2.py --force` ERROR=0 + 体积 ≥150KB + 页数 40-45；
3. `verify_interaction_v1.py --force` 无 ERROR（含 VIS-615 缺闭合、VIS-616 结构校验）——**本次转写不得重新引入缺闭合/采集断裂**；
4. 脚本核验：六课所有 `data-interaction-type` 属性在标签内；9 个基础判题函数（checkOpt/checkFill/checkOrder/checkLink/checkDrag/checkFlip/checkWrite/checkMatch）定义完整；
5. **浏览器实测抽查 2 课**（L09 + L26）：新题型（填空/排序/连线/拖拽）真实可操作、答题落 IndexedDB、双击撤销恢复、无 data-* 代码显示。

## 四、回报（简短 + 汇报落盘）

① 六课转写统计（每课转写多少题、新增哪些题型、选择占比从→到）② 动作种类表 ③ 双验证器回归 ④ 浏览器实测 ⑤ 已知偏差。

> **汇报落盘（必读）**：完成后将回报写入 **`D:\英语教学\00_总规划\05_交付与审核记录\许颖嘉B层六课多样题型_执行汇报_20260808.md`**（不存在则新建，已存在则覆盖为最新）。写好后**无需另行通知**，收口方定时读取该文件。
