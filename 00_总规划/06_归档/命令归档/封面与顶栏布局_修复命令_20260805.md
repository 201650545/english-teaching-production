# 封面与顶栏布局修复命令（2026-08-05 · 教师偏好）

**执行方**：可读取 `D:\英语教学` 本地文档的 Agent（原执行者）
**问题（教师反馈 2026-08-05）**：
1. 新重做课件封面把课时信息标签放到了**页面底部** → 要求**标签置顶、底部留白**；
2. 每个页面顶部有一个小标题条（如「📖 第07课时 · 阶段测试Ⅰ讲评」，`tb-title`）→ **以后每个课件每页顶部不再放这个小标题**（正文页已有自己的标题，冗余）；
3. 顶栏的**导出按钮** → **单个按钮放右上角**即可。

**范围（李民宪 1–5 + 邓兴华 7/8/9，全部按此规则处理）**：
- 邓兴华：`第07课时`、`第08课时`、`第09课时`（课件成品_网页PPT\第0X课时_课件.html）
- 李民宪：`第01课时` ~ `第05课时`（L4 目前缺失，随新规范生成时一并按此规则；其余按核验/修复处理）

> 说明：已确认新封面的（L07/L08/L09/L1）直接改；其余（L2/L3/L5 及待生成的 L4）在核验或生成时**同样应用这三条**。最终标准一致。

---

## 一、封面布局（三件 HTML）

- **顶部 = 徽标（cover-badge）+ 信息标签（cover-info）**；中部 = 标题/副题/标语（居中）；**底部留白**（不显示任何信息标签，可仅保留装饰 emoji 或清空）。
- 实现：`.cover-wrap`（flex column）子项 `order`：`cover-badge/cover-info → -3/-2`（置顶），`cover-title/sub/tagline → 0`，装饰 `cover-emoji → 10`（底部装饰可有可无）；或按同序重排 HTML。

## 二、页面顶栏（三件 HTML）

- **删除每页顶部的课程小标题**（`tb-title`，即「📖 第07课时 · 阶段测试Ⅰ讲评」）。
- 顶栏只保留**右上角**：导出按钮（单个，`export-btn`）+ 同步状态小点（`sync-dot`/`syncText`，数据采集反馈，可缩小为不占空间的提示，保留功能）。
- 顶栏高度相应精简；`pages-container` 的 `top` 偏移同步调整，正文页可视区尽量增大。

## 三、生成器同步（防复发，关键）

- 封面生成模板 + 顶栏模板（`courseware_engine.py` / `build_lesson_DXH_L07.py`、`build_lesson_DXH_L08.py`、`build_lesson_LMX_L01.py`）按以上规则改：
  - 封面：标签置顶、底部留白；
  - **每页顶部不再输出 `tb-title` 课程小标题**；
  - 顶栏 = 右上角导出按钮（+ 同步状态小点）。
- **影响所有后续新课**：许颖嘉/邓兴华/李民宪以后任何重出、新生成课件默认遵循。

## 四、规范补记

- `01_课件格式规范.md` 补两条（教师偏好 2026-08-05）：
  1. 封面：信息标签置顶、标题居中、底部留白；
  2. 页面顶栏：**不在每页顶部重复课程小标题**（正文页自带标题），顶栏仅右上角导出按钮 + 同步状态小点。
- 登记更新记录。

## 五、验收

- 浏览器抽查 邓兴华 L07/L08/L09、李民宪 L1（其余按同规则）：封面标签在顶部、底部留白；任意正文页顶部**无**「📖 第X课时…」小标题，页面标题（正文自带）正常；右上角有单个导出按钮；翻页/答题正常。
- 每件 `verify_v2.py --force` 仍 **ERROR=0**（改动不得破坏合同标记与页面契约）。

## 六、回报（简短）

① 改动文件清单 ② 三课件封面/顶栏核对结果 ③ 生成器与规范同步情况 ④ `verify_v2 --force` 结果 ⑤ 已知偏差。
