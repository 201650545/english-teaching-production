# 邓兴华 L07/L08 课件重构优化命令（2026-08-05 · 按新规范）

**执行方**：可读取 `D:\英语教学` 本地文档的 Agent（原执行者）
**范围**：邓兴华 **L07、L08** 两件课件，按新规范**重构优化**（内容保留，结构/视觉/交互升级）。**L09 不动**。
**优先级**：**L07 优先**（明天上课，尽快交付 L07，再做 L08）。

---

## 〇、输入材料（必读）

- `00_格式规范\04_分级差异化参数表.md` §四之四（题型配比/选择题占比硬线）
- `00_格式规范\01_课件格式规范.md`（§3.14 排版 / 主题化配色 / 六色卡）
- `00_总规划\_GPT交互菜单包_20260805\06_互动菜单与校验\interaction_menu_v1.json`（交互形式菜单）
- `00_工具\theme_colors.py`（主题化配色）；`00_工具\verify_v2.py`、`verify_interaction_v1.py`（含 VIS-613）
- 内容源：从 `邓兴华\第07课时\课件成品_网页PPT\第07课时_课件_中等.html`、`第08课时\...\第08课时_课件_中等.html` **提取知识点/题目/答案**，不虚构、不改知识点与正确答案。

## 一、视觉风格（打破橙红趋同）

1. **两课各用独立主色系**，不用红金默认：L07 = 蓝紫科技系，L08 = 绿原生态系（以 `theme_colors` 现有话题色为基础指定，或用 `:root` 变量覆盖主色）。
2. 封面模板 / 词汇卡变体按课取不同 variant（L07/L08 不重复同一变体）。
3. 结构仍用引擎统一骨架（`courseware_engine`），视觉差异来自 主色/背景纹理/封面/组件变体；不得用默认红金 `--brand` 全套。

## 二、交互形式（题型配比 + 菜单）

- **中等层硬线**：全课选择题 **≤45%**；语法/词汇/综合诊断热区 **≤35%**；纯识别 **≤50%**；全课主动作 **≥4 种**（点/拖/写/连/排/听）。
- 语法/词汇/随堂演练各环节按 `interaction_menu_v1.json` 选模板，动笔提取（写/填）与操作（拖/连/排）占主体，选择题只保留"四选一辨析"合适处，每页 ≤2 道。
- **阅读单选保留**（真题对位，不改原题），每 3 单选配 1 证据定位或每篇 ≥1 短答。
- 每题容器带互动合同元数据（`data-interaction-item`/`data-question-id`/`data-interaction-type`/`data-action-type`/`data-cognitive-level` 等）+ 页面 `<!-- CW-INTERACTION-CONTRACT:1 -->` 标记。
- 非选择交互必须**真实实现**（输入框/拖拽/连线/排序元素 + 判题 JS + saveAnswer 数据采集），严禁只贴元数据标签。

## 三、翻页豁免（防 L07 现 bug 复发）

- 文档级「左右半屏翻页」监听的豁免名单**必须包含** `.drag-container` / `.link-container` / `.order-container`（含其全部子元素），点击交互元素不翻页；左右半屏空白点击仍正常翻页。
- `verify_interaction_v1.py` 增加检查：交互容器类必须在翻页豁免名单中出现，否则 ERROR。

## 四、其他硬性要求

- 合同标记 + `verify_v2.py --force` 视觉 **ERROR=0** + `verify_interaction_v1.py --force` **ERROR=0**。
- 六色卡 6/6、防越级干净、数据采集（first/final/hint 区分）、exam_spec v2026.2、`courseware_core.py`/`CORE_CSS`/`CORE_JS` 禁改。
- 体积 ≥150KB、40–45 页、div 平衡、JS `node --check`。

## 五、验收

1. L07/L08 各跑 `verify_v2.py --force` 与 `verify_interaction_v1.py --force`：**均 ERROR=0**。
2. 选择题占比/热区/识别/动作种类达标（两口径）。
3. 浏览器实测：点 输入/拖拽/连线/排序 元素**不翻页**；判题正确；答错双击撤销；空白处左右翻页正常。
4. 视觉：L07 蓝紫、L08 绿系，两课主色/封面变体不重复、非橙红。
5. 阅读证据/短答落实，原题未改。

## 六、回报（简短）

① 模块×验收项 PASS/FAIL ② 变更文件清单 ③ 两课 选择占比/热区/识别/动作种类 实测 ④ 视觉主色与变体选用 ⑤ 翻页豁免实测 ⑥ 已知偏差。
