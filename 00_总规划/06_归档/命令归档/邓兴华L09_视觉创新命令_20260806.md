# 邓兴华 L09 · 视觉与结构创新命令（2026-08-06）

**执行方**：可读取 `D:\英语教学` 本地文档的 Agent（邓兴华线）
**缘由（教师反馈 2026-08-06）**：
1. L09 当前主色薄荷绿（`--brand:#06D6A0`）+ 青蓝（`--accent:#118AB2`），教师嫌**绿得丑**；
2. 课件结构看腻了（封面/页面千篇一律），要求**结构创新**；
3. 允许**不严格死磕验证器**的视觉层格式，让 Agent 自行根据知识点判断风格设计。

---

## 〇、现状

文件：`邓兴华\第09课时\课件成品_网页PPT\第09课时_课件_中等.html`（42 页）
知识点：特殊疑问句系统（G22 六疑问词）、选择疑问句（G23）、交通方位介词（G24）、20 新词（博物馆·交通·文化）、3 篇阅读 + 五选四。

## 一、视觉重设计（教师核心诉求）

### 1. 换掉薄荷绿主色（必改）
- 弃用 `#06D6A0` / `#04A87E` / `#7FEED5` 绿色系为**主体主色**。
- 新主色由你根据 L09 知识点自选，参考方向（可选其一，不强求）：
  - **博物馆/探索风**（L09 主题"博物馆之旅"）：暖黄+深棕/藏蓝，展品卡、地图、路标元素；
  - **交通/出行风**（交通方位介词）：靛蓝+灰白，站牌、路线图、地标元素；
  - **疑问句侦探风**（特殊/选择疑问句）：暗紫+琥珀，问号、线索卡元素。
- 整课主色统一、可用渐变/纹理，但**视觉主体不再是大面积绿**。

### 2. 结构创新（允许打破默认封面/页面布局）
- 封面：不强制 `cover-badge→cover-title→cover-meta` 固定顺序；可根据主题重排（如探秘海报式、站牌式、问号侦探式），信息标签位置自定，**但信息完整**（课次/知识点/时长/页数）。
- 页面：允许不同于其他课件的版式（双栏/卡片/时间线/路线图等），只要：
  - 保留 `class="page" id="pageN"` 契约（翻页/答题机制可用）；
  - 保留题容器与判题 JS、数据采集（IndexedDB）；
  - 每页可视区不拥挤、字号不低于可读下限（正文 ≥14px）。
- 鼓励创意：组件变体、进入动画、主题图标均可自由发挥，**只要知识点与交互真实**。

## 二、验证器口径（本次放宽视觉层）

- **硬规则保留**（必须满足，无法放开）：
  - 体积 ≥150KB、页数 40-45；
  - div 开合平衡、无脚本残留、JS `node --check` 通过；
  - 每题唯一 `data-correct`、答案分布 ≤40%；
  - `CW-INTERACTION-CONTRACT:1` + 题容器元数据；IndexedDB 数据采集可用。
- **视觉层放宽**：verify_v2 的视觉软检查（字号/颜色/圆角等 WARN、CSS_EXTRA 标记、cover 类名固定顺序）**不作为交付否决项**；视觉 ERROR 若来自"创新结构打破默认类名"，允许在回报中书面说明理由后通过。
- 目标：**ERROR=0 尽量满足，但创新导致的视觉类 WARN/ERROR 以书面理由放行**，不再死磕默认模板。

## 三、红线

- 不改知识点、题干事实、正确答案、阅读原文与正式原题；
- 交互必须真实实现（真 UI + 判题 JS + saveAnswer），**严禁只贴元数据标签换皮**；
- 不新增第三方库 / 第二数据库 / 联网依赖；
- 旧版 L09 归档 `_旧件_YYYYMMDD\`，不覆盖删除历史。

## 四、验收

1. `verify_v2.py --force`：硬规则项（体积/页数/div/答案分布/合同）ERROR=0；视觉层偏离项已书面说明；
2. `verify_interaction_v1.py --force` + VIS-613 DOM 实测：交互真实；
3. 浏览器实测：翻页/答题/数据采集正常；封面信息完整、新主色无大面积绿；
4. 回报中附：新主色值、封面/页面结构说明、视觉层放行项清单及理由。

## 五、回报（简短 + 汇报落盘）

① 改动文件清单 ② 新主色值 + 封面/页面结构设计说明 ③ 验证器放行项及书面理由 ④ 双--force 输出 ⑤ 浏览器实测 ⑥ 已知偏差。

> **汇报落盘**：完成后写入 `D:\英语教学\00_总规划\05_交付与审核记录\邓兴华L09视觉创新_执行汇报_20260806.md`，无需另行通知。
