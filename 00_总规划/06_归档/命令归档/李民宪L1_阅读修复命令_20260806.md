# 李民宪 L1 · 阅读三篇修复命令（2026-08-06 · 第二轮）

**执行方**：可读取 `D:\英语教学` 本地文档的 Agent（李民宪线）
**依据**：教师收口复核 + 实测判 L1 重做件仍有硬伤，须修复。

---

## 〇、现状（实测，非汇报）

文件：`李民宪\第01课时\课件成品_网页PPT\第01课时_课件_培优.html`（07:58 重做版）

| 检查项 | 实测 | 判定 |
|---|---|---|
| 主色紫系 / 交互合同 / quiz 占比 | 已达标（quiz-q 81→9） | ✅ |
| 阅读三篇词数 | 147 / 151 / 139 词 | ✅ 在 120-160 区间 |
| 阅读三篇**分段** | **全部 0 个 `<p>` 段落**，篇1/篇2 仅 1 个 `<br>` | ❌ |
| `{{source_id}}` 模板变量 | **3 处未替换**（reading_a / reading_b / sa）会显示在课件 | ❌ |
| C 篇难度 | 列表式（1. Choose… 2. Check…），无从句，培优档偏易 | ❌ |
| verify_v2 --force | 答案分布 A3/B4/C2，B=44% > ≤40% | ❌ FAIL |

---

## 一、修复要求

### 1. 三篇阅读按语义分段（必改）
- 每篇语篇正文拆成 **3-4 个 `<p>` 段落**，去掉 `{{source_id}}` 占位（改为规范 source_id 字段或注释，不显示在正文）。
- 段落语义清晰：篇1 按「出发→活动→结尾」，篇2 按「论点→论据→结论」，篇3 按「总起→分点（每条独立段落）→收尾」。
- 不得整篇挤在一个容器；段落间留白。

### 2. 修掉模板变量残留（必改）
- 清除正文中 3 处 `{{source_id:...}}` 明文（reading_a / reading_b / sa）。
- `source_id` 溯源应存为页面 `data-` 属性或规范字段，**不渲染在正文文本里**。

### 3. C 篇提升难度到培优档（必改）
- C 篇当前为「Holiday Safety Tips」列表式、全简单句。按培优档 B 阅读基线改写成**连贯语篇**：
  - 加入 1-2 处状语从句/宾语从句（不超八上语法上限）；
  - 保留 120-160 词；
  - 结尾可加一句因果/条件表达（if / because），使设问能考「主旨/推断/原因」而非纯细节。
- A/B 篇如教师判定偏简单，同步微调（保 120-160 词与生词率 ≤12%）。

### 4. 答案分布压回 ≤40%（必改）
- 当前 A3/B4/C2（B=44%）。调整干扰项或替换 1 题答案，使最大占比 ≤40%。

---

## 二、红线

- 不改知识点、正确答案、阅读原文事实；A/B 篇内容主体保留；
- 不新增第三方库 / 联网依赖；不动 L2-L5 与邓兴华/许颖嘉任何文件；
- 旧版 L1 归档 `_旧件_YYYYMMDD\`，不覆盖删除历史。

---

## 三、验收

1. `verify_v2.py --force` → **ERROR=0**（答案分布 ≤40%）；
2. `verify_interaction_v1.py --force` → **ERROR=0**；
3. 三篇阅读均 **3-4 个 `<p>` 段落**；正文**无 `{{source_id}}` 明文**；
4. C 篇改写成连贯语篇（含从句），A/B/C 词数均在 120-160；
5. 浏览器实测：正文分段显示正常、翻页/答题正常、C 篇设问可判题。

---

## 四、回报（简短 + 汇报落盘）

① 改动清单 ② 三篇段落数/词数/设问类型实测 ③ 答案分布 ④ 双--force 输出 ⑤ 已知偏差。

> **汇报落盘**：完成后写入 `D:\英语教学\00_总规划\05_交付与审核记录\李民宪L1阅读修复_执行汇报_20260806.md`，无需另行通知。
