# 邓兴华 L10-L12 · 课件交互修复 + 补练习命令（2026-08-06 第二轮）

**执行方**：可读取 `D:\英语教学` 本地文档的 Agent（邓兴华线）
**缘由（收口复核实测 2026-08-06）**：第一轮重构课件结构已达标（page-id 42 页、主色互异、体积达标），但**交互验证不通过**；L11/L12 配套练习未生成（执行 Agent 积分不足中断）。
**依据**：第一轮命令 `邓兴华L10-12重构命令_20260806.md` + 本命令修复。

---

## 〇、现状（收口复核实测，非汇报）

| 课时 | 课件 | 题量 | 交互缺陷（verify_interaction ERROR） | 练习 |
|---|---|---|---|---|
| L10 | ✅ 42页/173KB | 25 | **VIS-614**：翻页豁免名单漏 `link-container`/`order-container`（连线/排序点击误翻页）×1 | ✅ 已有 |
| L11 | ✅ 42页/187KB | 37 | **VIS-613**：fill_in 标签缺 input/textarea/contenteditable ×7（假交互）；**VIS-601-DOM**：选择题占比 47.2%>45%（25/53） | ❌ 缺 |
| L12 | ✅ 42页/180KB | 51 | **VIS-610**：28 处缺元数据（knowledge_id/section/template_id/interaction_type/action_type/cognitive_level）；**VIS-614**：豁免漏 `order-container`/`link-container`；**VIS-603**：动作种类 0<4 | ❌ 缺 |

> 课件结构（page-id/页数/体积/主色/交互合同）已达标**保留不动**，只修交互缺陷。

---

## 一、课件修复（三课，只修交互不动内容）

### L10 修复
- **VIS-614**：文档级点击翻页监听排除名单补 `link-container`、`order-container`（对齐 00_总规划 既有翻页豁免写法，参考 `邓兴华L07L09_翻页冲突修复命令_20260805.md`）。

### L11 修复
1. **VIS-613 假交互**（7 处 fill_in 缺 input）：Q11R_28_0/1/2、Q11_39_0 等——补真实 `<input class="fill-input">` + `checkFill` 判题 + `saveAnswer`；若本应是非填空交互（如排序/连线），改回正确标签。**严禁只贴元数据换皮**。
2. **VIS-601-DOM 选择题占比 47.2%>45%**：把低认知层级选择题转写为操作/生成类（拖拽/连线/排序/填空），降到 ≤45%；保留正式阅读单选。
3. 复扫确保六色卡 ≥4/6（现 5/6）。

### L12 修复
1. **VIS-610 缺元数据**（28 处）：补 `data-knowledge-id`/`data-section`/`data-template-id`/`data-interaction-type`/`data-action-type`/`data-cognitive-level`/`data-scorable`（对齐 L10/L11 现成写法）。
2. **VIS-614**：翻页豁免补 `order-container`/`link-container`。
3. **VIS-603 动作种类 0<4**：补足至少 4 种操作动作（select/write/drag/order/link/flip/assemble 等），把部分纯展示转交互。
4. 复扫六色卡（现 3/6，补到 ≥4/6）。

---

## 二、配套练习补生成（L11/L12 两课，中等档）

对齐 `第09课时_配套练习_中等.docx` / `第10课时_配套练习_中等.docx` 现成结构：

1. **结构对齐 exam_spec v2026.2**：阅读30 / 语言运用25 / 综合技能25 + 教学诊断附件（听力无）。
2. **难度**：中等档，不超过各课语法上限（L11: 情态/不规则复数/时间介词；L12: 冠词/量词/some-any）。
3. **溯源**：阅读真题母本改编 + `source_id`；双向细目表对齐 `{{SRC_*}}`。
4. **答案分布**：单选项最大占比 ≤40%。
5. **排版**：L05 紧凑模板、10.5pt、US Letter、首行缩进 1 字符。
6. **命名**：`邓兴华\第11课时\第11课时_配套练习_中等.docx`、`第12课时\第12课时_配套练习_中等.docx`。

---

## 三、红线

- 不改知识点、题干、正确答案、阅读原文、正式原题；
- 课件结构（page-id/页数/体积/主色）已达标**保持**，只修交互缺陷；
- 不新增第三方库 / 联网依赖；
- 改动前备份（L10-12 课件原结构版归档 `_旧件_第二轮修复前20260806\`）。

---

## 四、验收

1. 三课 `verify_v2.py --force`：ERROR=0（硬规则）+ 视觉 ERROR 书面放行；六色卡 ≥4/6；
2. `verify_interaction_v1.py --force`：**三课均 0 ERROR**（L10/L11/L12）；选择题占比 ≤45%；
3. 浏览器实测（每课）：翻页（含连线/排序页点击不误翻页）、答题、双击撤销、IndexedDB 采集正常；
4. 两 DOCX：四部分 100 分齐全、答案分布 ≤40%、结构/溯源核验通过。

---

## 五、回报（简短 + 汇报落盘）

① 三课修复明细（每课改了什么/补了什么）② 修复后 verify_interaction ERROR 数（应 0）③ 选择题占比实测 ④ 两练习结构/题量/答案分布 ⑤ 浏览器实测 ⑥ 已知偏差。

> **汇报落盘（必读）**：完成后将回报写入 **`D:\英语教学\00_总规划\05_交付与审核记录\邓兴华L10-12修复_执行汇报_20260806.md`**（不存在则新建，已存在则覆盖为最新）。写好后**无需另行通知**，收口方定时读取该文件。
