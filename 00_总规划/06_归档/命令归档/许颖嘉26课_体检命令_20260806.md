# 许颖嘉 26 课时 · 全量体检命令（2026-08-06 · 只体检不重做）

**执行方**：可读取 `D:\英语教学` 本地文档的 Agent（许颖嘉线）
**目标**：扫描许颖嘉 **26 课时**全量现状，产出一份体检报告；**本次只读不改，不产出任何课件/练习**。

---

## 一、体检范围

`许颖嘉\第01课时` ～ `第26课时` 全部课时目录。

## 二、体检项（逐课）

| # | 检查项 | 判定标准 |
|---|---|---|
| 1 | 课件是否存在 | `第XX课时\课件成品_网页PPT\第XX课时_课件_基础.html` 存在 |
| 2 | 配套练习是否存在 | `第XX课时\第XX课时_配套练习_基础.docx` 存在 |
| 3 | 契约是否齐全 | `契约\1_课程概要.md`～`6_动效与素材.json` 6 件齐 |
| 4 | 档位后缀规范 | 文件名带 `_基础`（许颖嘉=基础档） |
| 5 | 交互合同 | HTML 含 `CW-INTERACTION-CONTRACT:1` |
| 6 | 交互元数据 | 题容器含 `data-interaction-type` |
| 7 | verify_v2 --force | `python 00_工具\verify_v2.py --force <html>` ERROR=0 |
| 8 | 主题主色 | 有非红金主题（`--primary`/`--accent`/`THEME:`） |
| 9 | 体积/页数 | 体积 KB、页数（硬线 150KB+/40-45 页） |

## 三、产出

生成体检报告，存 **`00_总规划\05_交付与审核记录\许颖嘉26课_体检报告_20260806.md`**，表格每课一行，9 项逐项打 ✅/❌，末列「新规范/旧规范」标注：

- **新规范** = 有交互合同 + data-interaction-type + 主题化（如 L5/L17/L18/L26 重出件）
- **旧规范** = 无交互合同（抽查 L01/L06/L10/L15/L20/L24 均无，疑似同批旧件，需全量确认范围）

报告末尾附「待重做优先级」建议（如按课时顺序或教师待上顺序），**仅供教师决策参考，不自动执行**。

## 四、红线

- 只读，**不修改任何课件/练习/契约**；
- 不跑重做、不写回文件；
- verify_v2 仅运行不修改。

## 五、回报（简短）

① 体检报告文件路径 ② 新规范/旧规范 各几课 ③ 缺件清单 ④ 全部 verify 结果摘要 ⑤ 已知偏差。

> **汇报落盘（必读）**：完成后请将上述回报写入 **`D:\英语教学\00_总规划\05_交付与审核记录\许颖嘉26课体检_执行汇报_20260806.md`**（不存在则新建，已存在则覆盖为最新）。写好后**无需另行通知**，收口方定时读取该文件。
