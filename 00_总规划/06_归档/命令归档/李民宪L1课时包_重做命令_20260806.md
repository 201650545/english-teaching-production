# 李民宪 L1 课时包 · 重做命令（2026-08-06 · 收口复核判 FAIL 后补）

**执行方**：可读取 `D:\英语教学` 本地文档的 Agent（原执行者）
**依据**：收口复核实测 `李民宪\第01课时\课件成品_网页PPT\第01课时_课件_培优.html` 三处硬性不合格，须按 `00_格式规范\04` 培优参数重做。

---

## 〇、现状（收口复核实测，非汇报）

| 检查项 | 规范要求 | 实测 | 判定 |
|---|---|---|---|
| 主色 | L1=紫（五课独立，不用红金） | `--brand:#E63946` / `--accent:#FFD700`（红金） | ❌ |
| 交互合同 | `CW-INTERACTION-CONTRACT:1` + `data-interaction-type` 元数据 | 无 | ❌ |
| 选择题占比 | 培优 **≤30%**（全课选择题容器 / 总题容器） | ~63%（81 选择 / 128+） | ❌ |
| 操作类动作 | ≥5 种（点/拖/写/连/排/听） | sort/match/fill/hl-input 少量 | ⚠️ 不足 |
| verify_v2 --force | ERROR=0 | PASS | ✅（仅视觉层） |

> 说明：verify_v2 通过只代表视觉契约合格，**培优交互硬线（主色/交互合同/选择占比）未达标**，故整体判 FAIL。

---

## 一、重做要求（只改 L1，别动已 PASS 的 L2-L5）

### 1. 主色改为紫系（必改）
- 主题主色 `--brand` / `--accent` 换紫：如 `#7C3AED` / `#8B5CF6`（紫系），**去掉红金 #E63946/#FFD700 主体**。
- 其余四课（L2青/L3绿/L4琥珀/L5蓝）主色**不得改动**。

### 2. 补交互合同与元数据（必改）
- 页面加入 `<!-- CW-INTERACTION-CONTRACT:1 -->`。
- 每个交互题容器补齐 `data-interaction-type` / `data-action-type` / `data-cognitive-level` 等元数据（对齐 L2-L5 现成写法，可参照 `李民宪\第02课时\课件成品_网页PPT\第02课时_课件_培优.html` 作为结构范本）。

### 3. 选择题占比压到 ≤30%（必改）
- 将选择类（quiz）题按认知层级**转写成操作/生成类**：输入/拖拽/连线/排序/听写。
- 保留正式阅读单选（真题对位）；每 3 单选配 1 证据/短答。
- 全课主动作 ≥5 种；选择题只留"四选一辨析"合适处、每页 ≤2 道。

### 4. 档位 B 阅读基线（保持/核验）
- 语篇 120–160 词、生词率 ≤12%、三篇至少一篇任务型/问答、`source_id` 溯源。
- 若已达标保持不动，报告中附实测。

---

## 二、不变（红线）

- 不改知识点、题干事实、正确答案与阅读原文；
- 不动 L2-L5、邓兴华 7/8/9 任何文件；
- 不新增第三方库 / 第二数据库 / 联网依赖；
- 旧版 L1 归档 `_旧件_YYYYMMDD\`，不覆盖删除历史。

---

## 三、验收

1. `verify_v2.py --force` → **ERROR=0**；
2. `verify_interaction_v1.py --force` → **ERROR=0**（含 VIS-613 DOM 实测，反换皮：必须有真 UI + 判题 JS + saveAnswer 数据采集，**只贴元数据标签不算**）；
3. 选择占比实测 ≤30%（两口径一致）；主动作 ≥5 种；
4. 主色紫系、与 L2-L5 互异；
5. 浏览器实测：点 输入/拖拽/连线/排序 不翻页；判题正确；答错双击撤销；封面标签置顶、底部留白。

---

## 四、回报（简短）

① 改动文件清单 ② 改前/改后 选择占比、动作种类、主色 实测表 ③ 交互合同/元数据补齐情况 ④ 阅读三篇（词数/生词率/任务型）实测 ⑤ verify_v2 + verify_interaction_v1 双 --force 输出 ⑥ 已知偏差。

> **汇报落盘（必读）**：完成后请将上述回报写入 **`D:\英语教学\00_总规划\05_交付与审核记录\李民宪L1重做_执行汇报_20260806.md`**（不存在则新建，已存在则覆盖为最新）。写好后**无需另行通知**，收口方定时读取该文件。
