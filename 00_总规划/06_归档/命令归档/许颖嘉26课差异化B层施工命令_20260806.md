# 许颖嘉 26 课差异化 B 层 · 人工深化施工命令（2026-08-06）

**执行方**：可读取 `D:\英语教学` 本地文档的 Agent（许颖嘉线）
**依据**：`许颖嘉26课差异化_执行汇报_20260806.md`（A 层已全量完成）+ 本命令 B 层
**教师决策（2026-08-06）**：**按推荐六课施工**——L09 / L10 / L11 / L12 / L17 / L26。

---

## 〇、背景

A 层（脚本化基础换肤）已完成：26 课注入 5 个 retrofit recipe（R1-R5，连续三课不重复）、verify 26/26 PASS。B 层为**近期六课人工深化**——在 A 层基础上对**封面重新构图 + 关键组件变体 + 反馈语言 + 代表内容页局部版式**做深化，让观感更进一步区分。

## 一、六课深化清单（教师确认）

| 课时 | 主题 | 深化方向 |
|---|---|---|
| **L09** | 待上（七上进阶） | 封面重新构图 + 阅读页改时间线式 |
| **L10** | 待上 | 封面构图 + 选择题反馈语言"闯关成功" |
| **L11** | 待上 | 封面构图 + 交互组件变体 |
| **L12** | 待上 | 封面重构 + 内容页局部版式 |
| **L17** | 习惯主题（已上） | 反馈语言"证据确认" + 打卡式版式 |
| **L26** | 写作 SOP（重做） | 封面重构 + SOP 步骤条强化 |

## 二、每课深化内容（四件套，缺一不可）

1. **封面重新构图**：不止换色——调整封面布局（标题位置/主图比例/信息分区/装饰形态），使六课封面**互异且与 A 层其余课可区分**；
2. **一种关键组件变体**：选 1-2 个本课核心组件做版式变体（阅读页时间线/打卡式/步骤条/证据卡等，贴合主题）；
3. **反馈语言调整**：正误反馈文案随主题调整（如"闯关成功"/"证据确认"/"打卡完成"），**不得改判题逻辑**；
4. **代表内容页局部版式**：至少 2 页内容页局部版式变化（分栏/卡片/流程图等）。

## 三、红线

- 不改知识点、题干、正确答案、阅读原文、正式原题、判题 JS、IndexedDB 采集逻辑；
- 保持 page-id 契约、页数 40-45、体积 ≥150KB、交互合同、data-interaction-type、答案分布 ≤40%；
- 正误反馈语义色不可只用颜色区分（保留文字/图标/形状）；
- A 层已注入的 `<CW-DIFF-STYLE family="RX">` 标记**保留**，B 层在其上增量深化；
- 每课改动前备份到 `课件成品_网页PPT\_旧件_差异化B层20260806\`。

## 四、验收

1. 六课 `verify_v2.py --force` ERROR=0、`verify_interaction_v1.py --force` 无 ERROR；
2. 六课封面截图对比（B 层前后）确认重构生效且互异；
3. 浏览器实测抽查 2 课：翻页/答题/反馈语言/双击撤销/IndexedDB 正常；
4. 六课连续三课不重复校验仍 PASS。

## 五、回报（简短 + 汇报落盘）

① 六课各深化了什么（封面/组件/反馈/版式）② B 层前后封面对比说明 ③ 验证器结果汇总 ④ 浏览器实测 ⑤ 已知偏差。

> **汇报落盘（必读）**：完成后将回报写入 **`D:\英语教学\00_总规划\05_交付与审核记录\许颖嘉26课差异化B层_执行汇报_20260806.md`**（不存在则新建，已存在则覆盖为最新）。写好后**无需另行通知**，收口方定时读取该文件。
