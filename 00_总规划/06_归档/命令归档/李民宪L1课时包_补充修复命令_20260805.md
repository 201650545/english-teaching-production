# 李民宪 L1 课时包 · 补充修复命令（第二轮 · 2026-08-05）

**执行方**：可读取 `D:\英语教学` 本地文档的 Agent（原执行者）
**性质**：**定点补修**。第一轮四项中：SOP 前置（②）、拼读排版（③）已达标；阅读培优难度（①）、练习多样化（④）未完全达标；另有 1 处违反交付前强制。以下为须补修清单，其余保留。
**审核依据**：Claude 收口复核 2026-08-05 本地实测（verify_v2 --force + HTML 抽验）。

---

## 〇、审核结论（四项+强制逐一）

| # | 项 | 第一轮自评 | 收口复核结论 |
|---|---|---|---|
| 强制 | 交付前带合同标记 + 视觉 ERROR=0 | 未报告 | ❌ **违反批次2 §六.4**：`CW-VISUAL-CONTRACT:1` 与 `<CW-CSS-EXTRA>` 标记均缺失，`verify_v2 --force` 报 CSS-I001 ERROR。agent 用普通 verify（无标记跳过 CSS 检查）顶替才显示 PASS |
| 1 | 阅读培优难度 | ✅ | ❌ **过度自评**：三篇正文词数实测 A≈113 / B≈111 / C≈89，全部 < 档位B 下限 120（C 篇差 31 词）；三篇全为 quiz-opt 单选，§一.4「至少一篇任务型/问答」未落实；生词率 ≤12% 未报告 |
| 2 | SOP 移前 | ✅ | ✅ page28 SOP → page29/30/31 阅读A/B/C，确认 |
| 3 | 拼读排版重构 | ✅ | ✅ 分卡/音素高亮/对照词/口诀，确认 |
| 4 | 练习多样化 ≤50% | 🟡 未达标 | 🟡 确认 76% > 50%，未达标；「保留选择题每页≤2 道」亦未达成（语法闯关每考点6题/页） |

---

## 一、交付前强制（先修，本轮最高优先）

1. 课件 HTML 必须带 **`<!-- CW-VISUAL-CONTRACT:1 -->`** 合同标记 与 **`<CW-CSS-EXTRA>` / `<CW-SECTION ...>` / `</CW-CSS-EXTRA>`** 边界标记（`courseware_engine.py` `build_lesson()` 注入；`build_lesson_LMX_L01.py` 须走引擎注入路径，或重出后补标记）。
2. 验收**必须**用 `verify_v2.py --force` 跑：**视觉检查 ERROR=0（PASS）** 才算交付。普通 verify 因无标记会跳过 CSS 完整性检查，不得用作交付依据。

## 二、阅读三篇提到 120–160 词（档位B 下限）

- 现状实测：A≈113 / B≈111 / C≈89（含标题标称则 A119 / B119 / C94）。
- 要求：三篇正文均 **≥120 词**。A/B 各补 ~10 词，C 补 ~30 词——各扩写 2–3 个细节句（延续 Holiday Safety Tips 应用文体），不新增考点、不越语法上限、保持 source_id 溯源。
- 保持：四类设问每篇≥1 非细节、干扰项有效（近义/偷换/过度推断）、正确项唯一。

## 三、§一.4 题型：至少一篇改任务型/问答

- 现三篇全部 quiz-opt 单选。将 **C 篇（应用文）改为任务型阅读**：信息匹配 / 表格填空 / 判断正误 / 回答问题 任选，用输入式判题（`.fill-input-box`）或 GM 组件，**不得与 A/B 全是单选雷同**。若改任务型成本过高，至少 C 篇改「阅读回答问题」（简答输入判题）。

## 四、选择题占比压至 ≤50%（§四 硬阈值）

- 现状：quiz-opt 289 个，占比 76%。目标 ≤50%。
- 主战场：**语法闯关（每考点 6 题/页）+ 随堂演练（每页 6-10 题）**中约 100 道纯选择，改为 提取输入（看中文写英文，GM-V02 提示梯）/ 填空补全（词形变化）/ 排序重组 / 连线配对 / 改错（GM-G03）。保留的选择题每页 ≤2 道、每题带解析（§四.3）。
- 数据字段区分记录（08 规范）：`independent_correct` / `hint_used` / `transfer_correct`。

## 五、补验项（回报必须含数据）

1. 三篇阅读正文词数实测 + **生词率 ≤12%**（按 base_vocab + 750 矩阵口径计算，给数值）。
2. 选择题占比：quiz-opt 数 ÷ 交互总数，**须 ≤50%**。
3. `verify_v2.py --force` 完整输出，**视觉 ERROR=0** 才算 PASS。

## 六、验收

- `verify_v2.py --force` PASS 且视觉 ERROR=0；合同标记存在。
- 三篇阅读 ≥120 词、生词率 ≤12%、C 篇非纯单选。
- 选择题占比 ≤50%；保留选择题每页 ≤2 道且带解析。
- 浏览器抽查：SOP 在阅读 A 前、拼读页大字+音素高亮、任务型/问答页可作答、答题不翻页。

## 七、完成后回报（简短）

① 模块×验收项 PASS/FAIL（含本命令 〇 表逐项）② 变更文件清单 ③ `--force` 输出关键行 ④ 三篇词数/生词率实测 ⑤ 选择题占比 ⑥ 已知偏差。
