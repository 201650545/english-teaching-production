# 许颖嘉 26 课差异化 B 层 · 补足深化命令（2026-08-07）

**执行方**：可读取 `D:\英语教学` 本地文档的 Agent（许颖嘉线）
**缘由（收口复核实测 2026-08-07）**：B 层六课（L09/L10/L11/L12/L17/L26）虽有更新，但实测仅 ~1.6KB 微调（CW-DIFF 标记从 0→2），**未达到 B 层要求的深化深度**。教师决策：**要求补足深化**。
**目标**：六课补齐 B 层四件套（封面重新构图 + 关键组件变体 + 反馈语言 + 内容页局部版式），每课改动应**明显**（对比 A 层版本体积/组件有实质增量，非微调）。

---

## 〇、现状（收口实测）

- B 层六课在 2026-08-07 00:09 有更新，但对照 `_旧件_差异化20260806\`（A 层备份），改动极小（如 L09 仅 +1.6KB，journey=1 无 timeline）。
- B 层深化未真正落地：封面未重新构图、无关键组件变体、反馈语言未调整。

## 一、补足要求（六课，每课四件套必须实质落地）

| 课时 | 主题 | 深化方向（必做） |
|---|---|---|
| **L09** | 待上 | 封面重新构图（改 A 层版式）+ 阅读页改**时间线式**（.timeline 组件）+ 反馈语言调整 |
| **L10** | 待上 | 封面构图 + 选择题反馈语言"闯关成功" |
| **L11** | 待上 | 封面构图 + 交互组件变体（如填空改提示梯） |
| **L12** | 待上 | 封面重构 + 内容页局部版式（如点餐对话气泡） |
| **L17** | 习惯 | 反馈语言"证据确认" + 打卡式版式（.checkin 组件） |
| **L26** | 写作 SOP | 封面重构 + SOP 步骤条强化（.sop-steps 组件） |

**每课必须实现**：
1. **封面重新构图**：改变封面布局（标题位置/信息分区/装饰形态），与 A 层版本**可见不同**（对比截图可辨）；
2. **一种关键组件变体**：新增或改造组件（时间线/打卡/步骤条/对话气泡/提示梯），有独立 CSS + 结构，**能明显看出**（不只换色）；
3. **反馈语言调整**：正误反馈文案随主题（闯关成功/证据确认/打卡完成），**不得改判题逻辑**；
4. **内容页局部版式**：至少 2 页内容页局部版式变化。

**验收判定**：每课当前版本 vs `_旧件_差异化20260806\` A 层备份，`git diff` 或体积差应 ≥8KB 或新增组件类名（如 `.timeline`/`.checkin`/`.sop-steps`）出现。

## 二、红线

- 不改知识点、题干、正确答案、阅读原文、正式原题、判题 JS、IndexedDB 采集；
- 保持 page-id 契约、页数 40-45、体积 ≥150KB、交互合同、答案分布 ≤40%；
- A 层 `<CW-DIFF-STYLE family="RX">` 标记保留，B 层在其上增量；
- 每课备份到 `课件成品_网页PPT\_旧件_差异化B层补足20260807\`。

## 三、验收

1. 六课每课对照 A 层备份有**实质改动**（体积差或新增组件类名）；
2. 六课 `verify_v2.py --force` ERROR=0、`verify_interaction_v1.py --force` 无 ERROR 不回退；
3. 浏览器实测抽查 2 课：新组件渲染正常、反馈语言生效、翻页/答题/IndexedDB 正常；
4. 六课封面截图（A 层 vs B 层补足后）对比确认重构生效。

## 四、回报（简短 + 汇报落盘）

① 六课每课新增了什么组件/改了什么封面/反馈语言 ② A 层 vs B 层体积差与新增类名表 ③ 验证器回归 ④ 浏览器实测 ⑤ 已知偏差。

> **汇报落盘（必读）**：完成后将回报写入 **`D:\英语教学\00_总规划\05_交付与审核记录\许颖嘉26课差异化B层补足_执行汇报_20260807.md`**（不存在则新建，已存在则覆盖为最新）。写好后**无需另行通知**，收口方定时读取该文件。
