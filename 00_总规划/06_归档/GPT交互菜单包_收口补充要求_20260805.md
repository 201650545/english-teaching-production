# 收口补充要求（Claude 审核 2026-08-05 · 给执行 Agent）

**前提**：GPT《英语课件研发中心_V1_全量补发_2026-08-05.zip》整体方案可接收（交互形式菜单 + 按课轮换 + 选择占比硬线 + 互动校验，方向正确，未改 CORE、无第三方依赖、真题不改、阅读单选保留）。执行 **L07/L09 选择题重灾区改造**前，须追加以下要求；其余按 GPT `10_本地Agent命令\本地Agent执行命令_L07_L09选择题重灾区改造.md` 执行。

---

## 补充①：`verify_interaction_v1.py` 必须加 `--force` 模式

- **现状缺陷**：该脚本无 `--force`；HTML 无 `<!-- CW-INTERACTION-CONTRACT:1 -->` 标记时，所有 ERROR 降级为 WARN（其测试 `test_legacy_warn_only` 把此行为固化为预期）。这正是 2026-08-04 批次2 §六 修掉的 L26 事故同款坑——标记门控让真烂课件显示 PASS。
- **要求**：
  1. `inspect(html, level, force=False)` 加 `force` 参数；`force=True` 时忽略标记门控，ERROR/WARN 按规则判定，不降级。
  2. `main()` 加 `--force` CLI 参数，透传。
  3. 补测试：`--force` 下无标记的 BAD 件必须出 ERROR。
- **自验**：用 `--force` 跑当前（未带互动标记的）L07/L09 原件，应触发 VIS-601/602（全课/热区选择占比超标）ERROR——证明检查器能抓住旧件超占比。

## 补充②：交付前强制（对齐批次2 §六.4）

改造后的 L07/L09 必须**同时**满足，缺一不可：

1. HTML 带 `<!-- CW-INTERACTION-CONTRACT:1 -->` 标记；
2. `verify_interaction_v1.py --force` **ERROR=0**（全课选择≤45%、热区≤35%、纯识别≤50%、动作≥4 种等）；
3. `verify_v2.py --force` **视觉 ERROR=0**（沿用批次2 交付前强制，含合同标记检查）；
4. **阅读证据/短答落实**：这是新增任务，非仅选择题转换——每 3 道阅读单选至少配 1 个证据定位任务，或每篇至少 1 道短答/摘要；**不改变原题题干、选项与答案**，只在阅读页补任务。

两项 `--force` 都必须跑通，普通模式不得作为交付依据。

## 补充③：不要重复执行批次2

包内 `10_本地Agent命令\本地Agent执行命令_批次2_CSS完整性与视觉软检查_2026-08-05.md` 为**已闭环工作**（2026-08-04 已落地 `verify_visual_v1.py` + `visual_contract_v1.json` + `--force` 修复，L26 实跑 ERROR=0）。执行 Agent 只做本包 **L07/L09 选择题重灾区改造**，勿重复执行批次2 命令。

## 回报要求（简短）

① 模块×验收项 PASS/FAIL（含两条 `--force`）② 变更文件清单 ③ 改造前后对照：选择占比 / 热区占比 / 动作种类 / 识别占比 ④ 阅读证据/短答落实清单 ⑤ 已知偏差。
