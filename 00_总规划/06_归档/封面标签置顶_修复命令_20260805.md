# 封面布局修复：信息标签置顶、底部留白（2026-08-05）

**执行方**：可读取 `D:\英语教学` 本地文档的 Agent（原执行者）
**问题（教师反馈 2026-08-05）**：新重做的课件封面把课时信息标签（单元/主题/难度等，`cover-info`/`cover-badge`）放到了**页面底部**；教师要求**标签回到顶部，底部什么都不要**（旧版就是顶部标签）。

**范围**（三件新封面均中招，实测确认）：
- `邓兴华\第07课时\课件成品_网页PPT\第07课时_课件.html`（蓝紫）
- `邓兴华\第08课时\课件成品_网页PPT\第08课时_课件.html`（绿）
- `李民宪\第01课时\第01课时_课件.html`（紫）

---

## 一、封面布局修复（三件 HTML）

封面改为：**顶部 = 徽标（cover-badge）+ 信息标签（cover-info）；中部 = 标题/副题/标语；底部留白（无信息，可仅保留装饰 emoji）**。

实现方式二选一（推荐 CSS flex `order`，避免大改 HTML）：
1. **CSS order**：`.cover-wrap` 为 `flex-direction:column` 时，给子项设 `order`：`cover-badge`/`cover-info` → `-3/-2`（置顶），`cover-title`/`cover-sub`/`cover-tagline` → `0`，`cover-emoji` → `10`（留底部作装饰即可，或无则清空底部）。
2. **HTML 重排**：把 `cover-badge` 与 `cover-info` 移到 `cover-title` 之前。

要求：标题仍居中；底部**不显示任何信息标签**。

## 二、生成器同步（防复发）

- 封面生成模板（`courseware_engine.py` 的 cover 生成 / `build_lesson_DXH_L07.py`、`build_lesson_DXH_L08.py`、`build_lesson_LMX_L01.py`）按同一规则改——**未来任何重出/新课默认标签置顶、底部留白**。

## 三、规范补记

- `01_课件格式规范.md` 封面规则补一条（教师偏好 2026-08-05）：**封面信息标签置于顶部（徽标+课时信息），标题居中，底部留白**；并登记更新记录。

## 四、验收

- 浏览器打开三件课件封面：标签在顶部、底部无信息、标题居中、配色/封面变体不变。
- `verify_v2.py --force` 仍 ERROR=0（封面改动不得破坏合同标记）。

## 五、回报（简短）

① 改动文件清单 ② 三封面核对结果（标签顶部/底部留白）③ 生成器与规范同步情况 ④ `verify_v2 --force` 结果。
