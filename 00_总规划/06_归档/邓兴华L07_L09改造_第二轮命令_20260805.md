# 邓兴华 L07/L09 选择题重灾区改造 · 第二轮命令（2026-08-05）

**执行方**：可读取 `D:\英语教学` 本地文档的 Agent（原执行者）
**性质**：上轮判定**不合格**，按要求返工。以下为证据与本轮强制要求，冲突处本命令为准。

---

## 〇、上轮判定：不合格（本地实测证据，2026-08-05）

| 证据 | 实测结果 |
|---|---|
| quiz-opt 按钮数 | L07 **仍 365**、L09 **仍 292**，与改造前完全相同——旧选择题未替换 |
| 热区页现状 | L07 p28–40 每页仍是 **16 个四选一按钮**，仅在原 `quiz-q` 容器上叠了 4 条元数据 |
| 非选择交互落地 | 全课件 input 仅 1 个、无 contenteditable、无拖拽/排序 JS、无 write/order 判题函数——标注为 write/order/drag 的题**没有实现** |
| 上轮"占比 26.6%/32.1%" | 是校验器只看元数据标签算出的**假值**：64/28 条 data-item 被标签成 17 条 choice + 其余 write/order，但真实 DOM 全是四选一按钮 |
| 上轮 --force | 补充①补丁已打（00_工具 版有 force），但校验器信标签不信 DOM，被换皮骗过 |

**结论**：本轮要求**真实转换**，禁贴标签。

## 一、真实转换（核心）

热区页（L07 p14/p28–40；L09 p22/24/26/29/34/35/37）的选择题必须**实际替换**为可用的非选择交互，且**删除原四选一按钮**（该题只保留新交互）：

| 标注类型 | 必须真实渲染 | 判题 JS | 数据采集 |
|---|---|---|---|
| write | `<input class="fill-input-box">` 或 contenteditable | checkInput | saveAnswer + hint 分级 |
| order | 可点击/拖拽排序的词块序列 | orderCheck | saveAnswer + attempt |
| drag | draggable 或"点源→点目标"替代 | dragCheck | saveAnswer |
| link | 连线/配对元素（题目↔依据） | linkCheck | saveAnswer + 配对记录 |

阅读单选按 GPT 方案保留（原题不改）；每 3 单选配 1 证据/短答。

## 二、给校验器加 DOM 实测（关键反作弊，必做）

在 `verify_interaction_v1.py` 新增 **VIS-613 交互类型-DOM 匹配（ERROR）**：

- 对每个 `data-interaction-item` 容器，按其 `data-interaction-type` 检查容器内**真实 DOM**：
  - `choice` → 允许 `button.quiz-opt`；
  - `fill_in/write` → 必须含 `input`/`textarea`/`contenteditable`，否则 **ERROR**；
  - `order` → 必须含可排序元素（draggable/order-item/点击换位），否则 **ERROR**；
  - `drag` → 必须含 `draggable` 或"点源→点目标"元素，否则 **ERROR**；
  - `link` → 必须含连线/配对元素，否则 **ERROR**。
- 容器内只要有 `button.quiz-opt`，该容器按 **choice** 计（不认标签）。
- **占比重算**：choice = 真实 quiz-opt 按钮数；非选择 = 真实输入/拖拽/排序/连线元素数。按此口径再算全课占比与热区占比，须 ≤45% / ≤35%。

## 三、交付前强制（本轮）

1. L07 quiz-opt 按钮总数较 365 **显著下降**（= 阅读保留 + 非热区每页 ≤2 后的总量）；L09 同理。热区页必须出现真实可点的非选择交互。
2. `verify_interaction_v1.py`（含 VIS-613）`--force` **ERROR=0**（两个口径占比都达标）。
3. `verify_v2.py --force` 视觉 **ERROR=0**。
4. 浏览器实测：write/order/drag/link 交互真实可用、判题正确、答错双击撤销、点击不翻页。
5. 数据导出含互动元数据，`first_answer/final_answer/hint_used` 区分。

## 四、回报（简短）

① 模块×验收项 PASS/FAIL（含 VIS-613、两口径占比）② 变更文件清单 ③ 改造前后 quiz-opt 按钮数 + 真实 DOM 选择占比（两口径）④ 阅读证据落实 ⑤ 已知偏差。
